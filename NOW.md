# NOW

Goal: 主动关心你的身体搭子 - 健康关怀 AI 应用
Status: active
Current: Phase D 完成 ✅, Phase 4-A (推理树 + Expand API) 完成 ✅, 交互式信息搜集 (followup_question + followup_options 选择题形态) 后端+前端全部完成; 追问后新增建议「一键全部采纳」横幅已实现; §8 Agent 编排的确认草案已就绪 (docs/agent-boundaries.md + docs/safety-hard-rules.md); 新增开放问题 #1 的技术分析 (docs/agent-communication-analysis.md); 2026-09-11 完成 user_state.json 损坏/遗留数据加固 (读路径字段清洗 + 写路径容错 + 首个 tests/test_user_state.py); 同日完成 4 个此前未覆盖模块的同类反序列化加固 (prompt_traces 顶层/条目 / storage 故事缓存嵌套 / mrt_engine store+decisions / exam_graph load); 2026-09-04 完成 plan_reminders.json 提醒 CRUD + scheduler_state.json load + _decide_proactive_type 三处损坏/遗留数据加固; 2026-09-03 完成 write-path (update/expand/set_followup) 反序列化加固补全 + legacy categories/context_snapshot 守卫 + feedback.py 损坏条目加固; 2026-08-31 完成 psycho reason 字段清洗 + 反序列化 children/reasoning_tree 健壮性加固 + README 路由数校准; 2026-08-30 完成 followup_options 写入归一化 + passive 推理输出 schema 修复 + expandable 不变量源头收口 + expand_node 空展开 no-op 守卫; 2026-08-27 完成 update_leaf_status 撤销完成一致性修复 + expand_node 幂等性测试补全; 2026-09-12 完成 persona.json 读路径加固; 2026-09-13 完成 trajectory_graph 读路径加固 (load_events / load_trajectories); 2026-09-14 完成 insights 读路径加固 (非 list 容器 / 非 dict 消息条目 / 非 dict extracted_data 三类崩溃路径); 2026-09-15 完成 routine.py 读路径加固 (非 list 消息容器 / 非 dict 条目 / sleep·exercise 非数值求和 / leaf 非字符串字段)
  ✅ 2026-09-15: routine.py 读路径加固 — ① learn_from_messages 此前对 storage 传入的 messages 零校验: 容器非 list (None/dict/str/int) 时迭代抛 TypeError, 条目非 dict 时 _extract_from_message 的 msg.get 抛 AttributeError (/api/routine、/api/anomalies、/api/chat 及计划生成 500)。② extracted_data 中 schema 约定为数值的字段 (sleep.quality / sleep.duration_hours / exercise.duration_minutes) 若为字符串 ("7.5"/"good") 或 dict 会原样入 sleep_times/exercise_records, 随后 _build_routine_flags 与 detect_anomalies 的 sum(...) 抛 TypeError; exercise.type 为 dict 时 set(exercise_types) 抛 TypeError (unhashable), 非字符串 type 还会漏进 routine summary 响应。③ check_plan_adherence 的 frequency 为 int/bool 时 `"每周" in frequency` 抛 TypeError, 且 id/label/action_text/time_slot 为非字符串时原样漏进返回结果。现新增 _message_dicts (非 list 容器退化空表、跳过非 dict 条目) / _sanitize_number (仅接受非 bool 的 int/float) / _sanitize_str 三个 helper: 提取期清洗 + 聚合期 (两处 sum / set) 与 leaf 字段兜底。良构数据行为逐字段不变 (summary/patterns/anomalies/adherence 全等, 含 quality=0 边界)。新增 tests/test_routine.py::TestRoutineRobustness 16 tests (其中 13 条修复前必失败)。484→500 tests 全过, py_compile 通过
  ✅ 2026-09-14: insights.py 读路径加固 — compute_correlations / get_trend_insights 此前对 storage/API 传入的 messages 零校验: ① 消息条目为非 dict (str/int/None/list) 时 `msg.get` / `m.get` 抛 AttributeError; ② messages 容器非 list (None/dict/str) 时迭代键或抛 TypeError; ③ 消息的 extracted_data 为非 dict (str/list) 时, get_trend_insights 的 `_safe_num(m.get("extracted_data", {}).get("mood"))` 链抛 AttributeError (而 _extract_score_pairs 早已 isinstance 守卫, 属同类不一致)。上述路径在 /api/chat 上下文、body story、scheduled_stories 等端点 500。现新增 _message_dicts (非 list 容器退化为空表、跳过非 dict 条目) 与 _extracted_data (extracted_data 非 dict 归 {}) 两个 helper, 两入口统一复用。良构消息行为逐字段不变 (correlation + trend 输出断言)。新增 tests/test_insights.py 10 tests (良构输出 2 / 非 list 容器退化 / compute 跳 junk 保良构 / trend 跳 junk 仍出趋势 / 全 junk 空 / 非 dict extracted_data 两入口 / 缺字段 / 非数值 mood)。474→484 tests 全过, py_compile 通过
  ✅ 2026-09-13: trajectory_graph 读路径加固 (load_events + load_trajectories) — ① load_events 此前对 storage 返回数据零校验: per-user 桶为非 list (dict/str/int) 时遍历其键/字符、条目为非 dict 时 item.get 抛 AttributeError; 条目为 dict 但 dimension 为未知字符串时 EventDimension(...) 抛 ValueError (trajectory API 500); metadata 为非 dict、label/raw_text/source_message_id 为非字符串、value 为非数值 (str/dict) 时原样入 EventNode 泄漏进 get_timeline 响应, raw_text 非字符串还会在 to_dict 的 [:200] 抛 TypeError, value 非数值在 detect_periodicities 的 sum/算术处抛 TypeError。现新增 _sanitize_str / _sanitize_optional_number / _sanitize_count / _sanitize_dict / _sanitize_str_list / _sanitize_dimension / _sanitize_period_type (与 user_state/scheduler 既有 _sanitize_* 模式一致): 非 list 容器退化空表、非 dict 条目跳过、非法枚举键回落默认 (events: mood, trajectories: energy/daily)、越界字段清洗。② load_trajectories 原 catch 元组仅 (ValueError, KeyError), 非 dict 条目 .get 抛 AttributeError 会击穿; 现同样跳过并清洗 confidence/stability/sample_count/evidence_event_ids/period_unit。良构数据行为逐字段断言完全不变。新增 tests/test_trajectory_graph.py::TestTrajectoryStorageRobustness 8 tests。466→474 tests 全过, py_compile 通过
  ✅ 2026-09-12: persona.json 读路径加固 — persona._load_user_persona 原先只捕获 JSONDecodeError/IOError 且无 schema 守卫: ① persona.json 顶层为合法 JSON 非对象 (list/str/int/null) 时 data.get 抛 AttributeError (未被捕获, get_active_persona 在 /api/chat 等多端点 500); ② persona_id 为非字符串 (list/dict) 时原样返回, 一路传进 get_persona 的 PRESET_PERSONAS.get() 抛 TypeError (unhashable), 不回落默认人设而是崩溃。现新增 _sanitize_persona_id (仅接受 str, 其余归 None), 顶层非 dict 记 warning 并归 None, catch 元组补 TypeError。良构 persona_id 字符串行为完全不变 (原样返回并正确生效)。新增 tests/test_persona.py::TestPersonaConfigRobustness 12 tests (非对象顶层 5 参数化 / 非字符串 id 4 参数化 / 缺 persona_id / 良构 id 不变 / 损坏 JSON)。454→466 tests 全过, py_compile 通过
  ✅ 2026-09-11: 反序列化加固扩展到 4 个此前未覆盖模块 (与 user_state 加固并行) — ① prompt_traces._read_all: prompt_traces.json 顶层为非 dict (list/str/int/null) 或条目值非 dict 时, get_trace / get_recent_traces / save_trace 的 .get()/​.values()/排序抛 AttributeError (traces API 500); 现顶层非 dict 退化空表、非 dict 条目过滤, get_trace 再兜底 isinstance。② storage 故事缓存: _read_story_cache_nolock 原先仅保证顶层 dict, 嵌套 user bucket / period 值为非 dict 时 get_story_cache 的 .get() 抛 AttributeError、set_story_cache 的 setdefault 赋值失败; 现读路径丢弃损坏 bucket/period 值, get/set 各自兜底修复。③ mrt_engine: mrt_data.json 顶层为非 dict 时 save() 的 store[user_id]= 抛 TypeError; decisions 非 list / 含非 dict / 缺必填字段的条目在 MRTDecision.from_dict 抛 AttributeError/TypeError; 现顶层非 dict 退化 {}, load 仅接受 dict 记录、list decisions, 畸形条目跳过。④ exam_graph.load_exam_graph 此前完全无 try: 损坏/半写入文件抛 JSONDecodeError 500; 现损坏或非 dict 返回 None。另 storage._compute_data_hash 跳过非 dict/缺 timestamp 消息。良构数据行为完全不变。新增 17 tests (test_prompt_traces.py 4 / test_storage_robustness.py 7 / test_mrt_engine.py 4 / test_exam_graph.py 2)。437→454 tests 全过, py_compile 通过
  ✅ 2026-09-11: user_state.json 损坏/遗留数据加固 — user_state.json 手改/部分写入时, 某 user 条目内的字段类型损坏会同时击穿读路径与写路径: ① get_user_state 原样透传 raw dict, 于是 interaction_count 为 "abc"/dict、avg_reply_length 为 dict、baselines 为 str/list、recent_*_trend 为 dict、user_preferences/routine_summary 为非 dict、profile_notes 为 dict 等全部泄漏进 API 响应与 prompt schema; ② update_user_state 对这些值做 int()/float()/dict(state.get("user_preferences")) 时抛 ValueError/TypeError (聊天 /api/chat 在持久化用户状态处 500); ③ _update_baseline/_trend_label 对 LLM 返回的非数值 sleep/energy/mood (str/dict) 做 float() 同样抛错。现新增 _sanitize_count / _sanitize_optional_float / _sanitize_optional_str / _sanitize_dict, get_user_state 经 _sanitize_state 统一清洗已知字段, _update_baseline/_trend_label 内部对前后值再兜底清洗。良构数据行为完全不变 (计数/EMA/趋势标签逐一断言)。新增 tests/test_user_state.py 8 tests (读路径损坏字段修复 / 良构值保留 / 非 dict 条目退化默认 / 写路径容忍损坏状态 / 非数值提取忽略 / 良构 EMA+计数 / legacy 字符串计数 / 计数边界)。429→437 tests 全过, py_compile 通过
  ✅ 2026-09-04: plan_reminders.json + scheduler_state.json + 消息列表 损坏/遗留数据加固 — ① health_plan 提醒 CRUD (list_reminders / create_reminder / delete_reminder / delete_plan 的提醒清理): plan_reminders.json 手改/部分写入时, 某 user 下的容器若为非 list (dict/str/int/null), list 迭代 .get() 或 create 的 .append() 会抛 AttributeError/TypeError; list 内非 dict 条目或缺 id 的 dict 会在 _dict_to_reminder 的 d["id"] 抛 AttributeError/KeyError (reminders API 500, 提醒 daemon 静默停发)。现新增 _is_reminder_dict (跳过非 dict / 缺可用 id 的条目) + _reminders_container (非 list 容器按空表处理, create 时重建为 list), 四个入口统一加固。② scheduler.SchedulerState.load: scheduler_state.json 顶层为非 dict 的合法 JSON (list/str/int/null) 时 state.__dict__.update 抛 ValueError/TypeError 且未被捕获; 字段类型损坏 (计数为 str/None/float、时间戳为 dict/int) 会污染 get_scheduler_status 响应并在后续 >= / += / fromisoformat 处抛错; 现顶层非 dict 退化为全新 state, 计数经 _sanitize_count 归为非负 int, 可选字符串字段经 _sanitize_optional_str 归 None。③ scheduler._decide_proactive_type silence 检查: messages[-1] 为非 dict 条目时 .get() 抛 AttributeError (catch 元组不含 AttributeError, 会击穿 generate_proactive_message 的 Tier 2); 现从尾部回找最近一个良构 dict 消息, 全 junk 则自然回落。良构数据行为完全不变。新增 9 tests (reminder list 跳 junk 保序 / 损坏容器退化 / delete 跳 junk 仍删真条目 / create 修复损坏容器 / delete_plan 清理跳 junk; state 顶层非 dict 退化 / 字段类型清洗; _decide_proactive_type 跳过非 dict 消息 / 全 junk 回落 general)。420→429 tests 全过, py_compile 通过
  ✅ 2026-09-03: feedback.py 损坏条目加固 (独立于 write-path 加固的并行会话) — feedbacks.json 手改/部分写入时, list_feedbacks 对 list 中非 dict 条目 (字符串/数字/null) 调 .get() 抛 AttributeError、对缺 created_at 的条目排序抛 KeyError (均 500); update_feedback/delete_feedback 对非 dict 条目取 fb["id"] 同样崩溃。现新增 _is_feedback_dict 守卫: list/update/delete 三个入口跳过非 dict 条目, list 排序改用 .get("created_at", "") 兜底, 损坏条目不再让 feedback 列表/更新/删除整体 500 (与 health_plan children 跳过 junk 的同类策略)。良构数据行为完全不变。新增 4 tests (list 跳过非 dict 保留良构保序 / 缺 created_at 排最后不崩 / update 跳过 junk 仍更新真条目 / delete 跳过 junk)。416→420 tests 全过, py_compile 通过
  ✅ 2026-09-03: 反序列化加固补全——write-path 与 legacy 转换三处同类不一致 — ① health_plan._find_node_in_dict_tree (update_leaf_status/expand_node/set_followup_question 直接操作文件原始 dict 树, 绕过 _dict_to_node 归一化): 遗留/损坏 JSON 的 children 为 dict/null 或含非 dict 条目/非 dict 子树时, 旧实现会对非 dict 调 .get/迭代 dict 键而 500; 现非 dict 子树返回 None、非 dict 子条目跳过、非 list children 不再下探 (与 08-31 read-path 清洗对齐)。② _dict_to_plan legacy categories 加 isinstance list 守卫 (truthy 但损坏的 dict categories 以前会在 get_plan/list_plans 崩溃), 损坏时退化为无树; ③ _convert_legacy_categories_to_tree 内部跳过非 dict category/item、items 非 list 视为空、非 list categories 返回空根; ④ _dict_to_plan context_snapshot 非 dict (str/list) 归 {} (None 与缺失行为不变, 防 regenerate 流程 .get 500)。良构数据行为完全不变。新增 9 tests (write-path 三个入口容忍损坏兄弟节点 / categories dict·list 损坏退化 / legacy 跳过 junk / context_snapshot 清洗·None·缺失保留)。407→416 tests 全过, py_compile 通过
  ✅ 2026-08-31: 两个低风险健壮性加固 + 文档校准 — ① psycho_engine._normalize_psycho_extracted 的 reason 字段清洗: 文档约定为 str|None, 旧实现原样透传 raw["reason"], LLM 若返回 dict/list/数字会污染输出 schema (下游 PsychoEvent.reason 类型为 str); 现非 str 统一 sanitize 为 None, 良构数据 (str/None/缺失→"") 行为完全不变。② health_plan._dict_to_node 反序列化加固: 遗留/损坏 JSON 的 children 若为 null/dict 或含非 dict 条目, 旧实现会在 get_plan/加载时抛 TypeError/AttributeError (500); 现非 list 归空、非 dict 条目跳过; 同步给 _dict_to_plan 的 reasoning_tree 加 isinstance dict 守卫, 损坏的树退化为 None (可回退 legacy categories)。③ README 目录注释路由数 19→65 (app.py 实际 65 个路由注册 / 56 个唯一路径)。新增 8 tests (reason 字符串透传/缺失默认空串/null 保留/结构化垃圾 sanitize 为 None; children null/dict/混合 junk 归空或跳过; reasoning_tree 非 dict 退化 None)。399→407 tests 全过, py_compile 通过
  ✅ 2026-08-30: 推理树两个低风险健壮性加固 — ① ai_engine.expand_reasoning_node 在源头派生 child["expandable"] = not is_actionable: 展开 prompt 不输出 expandable 字段, 此前靠 app.py 事后 setdefault 补, 直接调 expand_node 的调用方会让 actionable 叶子拿到 expandable=True (前端会显示不可用的展开钮); 现在源头收口, app.py 的 setdefault 变为无害 no-op。② health_plan.expand_node 空 children_data 改为 no-op: 旧实现会把节点标记 expanded=True 且 0 子节点, 从而永久锁死后续展开 (瞬时 LLM 失败返回空列表时尤其危险); 现空展开不写库、不改 expanded, 调用方可重试。良构数据行为完全不变。新增 2 tests (空展开 no-op 后可正常再展开 / actionable→expandable False + non-actionable→expandable True)。397→399 tests 全过, py_compile 通过
  ✅ 2026-08-30: 两个低风险健壮性修复 — ① health_plan.set_followup_question 补上 followup_options 写入归一化 (复用 _normalize_followup_options; 08-26 加固已覆盖 _dict_to_node 与 expand_node 写库, 唯独漏掉此写入口 — 调用方若传入结构化垃圾/非 list 会原样入库并漏进序列化与 API 响应)。良构数据行为完全不变。② psycho_engine.infer_passive_psycho_signals 移除结果 dict 中不属于 PSYCHO_DIMENSIONS 的游离键 "social_pressured" (silent 模式已由 avoidance 表达, 该键不进入下游 schema, 只会污染中间 dict, 违反输出契约)。新增 2 tests (set_followup_question 选项清洗保序 / silent 模式输出 schema 干净: 仅含 9 维 + phase/reason)。395→397 tests 全过, py_compile 通过
  ✅ 2026-08-27: update_leaf_status 撤销完成一致性修复 — 叶子从 "done" 改回 "pending"/"skipped" 时, 旧实现残留 completed_count 与 last_completed (叶子已非 done 但计数/时间仍停留)。现改为撤销完成时递减 completed_count (下限 0) 并清空 last_completed; "done" 时递增语义完全不变 (test_mark_done_twice 仍过)。新增 4 tests: 撤销清除计数与时间戳 / done→skipped 同理 / done→pending→done 只计 1 次 / 非 done 改 pending 不动计数。另补 expand_node 幂等性测试 (double-expand 不产生重复子节点, 含磁盘重载复核)。390→395 tests 全过, py_compile 通过
  ✅ 2026-08-26: §8 开放问题 #1 技术分析 — 新增 docs/agent-communication-analysis.md: 对比同步 pipeline vs 异步消息队列 vs 事件驱动 三方案在 Viora 的具体代码上下文 (单进程 Flask、无独立 broker), 推荐「同步 pipeline 主干 + 同进程轻量事件通知 (回调注册表) 旁路触发」, 与 agent-boundaries.md #1 推荐默认值一致; 给出 AgentMessage dataclass (含 trace 追溯链)、Orchestrator.run() 骨架、EventBus 回调注册表、与现有模块接线表; 无新依赖 (仅新增 orchestrator.py + event_bus.py)。纯分析文档, 不进入实现, 供用户确认 §8 用。388 tests 状态不变 (未改代码)
  ✅ 2026-08-26: followup_options 健壮性加固 — ① health_plan 新增 _normalize_followup_options, 在反序列化 (_dict_to_node) 与 expand_node 写库两处统一清洗: 非 list→None、丢弃 dict/list/tuple/set/None/空白、标量 str 化 (防止遗留/损坏 JSON 把非 list 值漏进序列化与 API 响应, 前端 Array.isArray 守卫形同虚设) ② ai_engine.ask_followup_question 选项过滤结构化垃圾 (dict/list/tuple/set/None), 避免 str() 把对象 repr 当选项漏进 UI。行为对良构数据完全不变。新增 2 tests (反序列化清洗 / LLM 选项过滤)。388→390 tests 全过
  ✅ 2026-08-22: 修复 update_leaf_status 静默无操作 bug — 目标节点在推理树中不存在时, 旧实现仍返回 plan (调用方无法区分"未找到"与"成功更新", adopt/update-item API 的 404 分支形同虚设)。现改为未找到计划或节点时返回 None, 使 API 正确返回 404。新增 3 tests (missing leaf → None / missing plan → None / adopt missing leaf → None)。384→387 tests 全过
  ✅ 2026-08-22: followup_options 去重 — ai_engine.ask_followup_question 选项归一化改为 dict.fromkeys 保序去重 (LLM 可能输出重复/近似选项; prompt 要求互斥, 重复是噪音), 再走原 strip/去空/上限6。新增 test_options_deduplicated_preserving_order。387→388 tests 全过
  ✅ 2026-08-19: 追问后一键采纳新叶子 — ① health_plan 新增 list_new_actionable_leaves(plan, known_ids) (收集 plan 中不在已知集合内的可采纳叶子, 精简字段: id/label/emoji/time_slot/frequency/duration) ② app.py expand 端点在展开前记录 before_actionable_ids, 展开后用 list_new_actionable_leaves 计算新增可采纳叶子并返回 new_actionable_leaves (追问答完后展开同样生效) ③ index.html 新增 showNewLeavesBanner + adoptLeaves — 展开产生新建议时显示可忽略横幅「🎯 本次展开新增 N 条可采纳建议」, 「全部加入计划」批量调用 adopt 端点后 renderPlan+重渲染树, 「忽略」关闭; 新增 .new-leaves-banner CSS。384 tests 全过 (+3: list_new_actionable_leaves 按 known_ids 差分 / 非可采纳过滤 / 精简 dict 无 children)
  ✅ 2026-08-18: 追问升级为选择题形态 — ① ReasoningNode 新增 followup_options (list[str]); 序列化/反序列化/set_followup_question 全链路支持; 展开时随 followup_question 一并清除 ② prompt_tree plan/expand_followup 模板要求 LLM 输出结构化 options 数组 (2-4 个互斥选项) ③ ask_followup_question 改为返回 (question, options) 元组; options 清洗+去空+上限6 ④ app.py expand 端点透传 followup_options; _plan_to_response.serialize_node 补上 followup_question+followup_options (修复 ❓ 标记/节点详情此前拿不到追问字段) ⑤ index.html 追问卡片+节点详情面板渲染选项胶囊按钮 (点击填充/再点取消, Enter 提交), 新增 .followup-option CSS。381 tests 全过 (+5: options roundtrip/清除/持久化/归一化/legacy 兼容)
  ✅ 2026-08-14: 前端接入交互式追问 UI — index.html: ① expandNode 支持 answer 参数并处理 needs_answer 响应 ② 新增 showFollowupPrompt/hideFollowupPrompt 浮动追问卡片 (问题+文本输入+提交/取消) ③ showNodeDetail 对带 followup_question 的节点显示回答输入框 ④ 推理树渲染对带待答追问的节点加 ❓ 标记。JS 语法校验通过 (node --check); 376 tests 全过
  ✅ 2026-08-13: 实现 Phase 4 待完善方向「交互式信息搜集」 — ReasoningNode 新增可选 followup_question 字段; ai_engine.ask_followup_question() 判断展开前是否需追问; expand_reasoning_node 支持 user_answer 注入; /api/plan/<id>/reasoning/expand 支持 answer 参数 + 返回 needs_answer; set_followup_question() 持久化。13 个新测试, 363→376 tests 全过
  ✅ 2026-08-11: §8.5 前置确认文档草案生成 — docs/agent-boundaries.md (四大 Agent 详细职责边界 + 7 项开放问题推荐默认值) + docs/safety-hard-rules.md (9 条安全硬规则 + 实现分层)。不进入实现，仅供用户确认；确认后即可开发编排层
  ✅ 2026-08-10: 新增 tests/test_feedback.py (9 tests) — feedback.py 全量 CRUD + 损坏 JSON 自动恢复 + 锁; 新增 pytest.ini 消除 asyncio 弃用警告; 354→363 tests 全过
  ✅ ReasoningNode 数据模型 + 序列化 (health_plan.py)
  ✅ Expand prompt + expand_node() (ai_engine.py)
  ✅ Expand API + generate 改造 (app.py)
  ✅ D3 推理树渲染 + 节点交互 (index.html)
  ✅ 叶子节点详情面板 + 采纳逻辑 + 聚合列表视图
  ✅ 聊天 plan_action 通知 + 推理路径展示
  ✅ 计划提醒 Agent (scheduler + CRUD)
  - 500 tests 全过 (2026-09-15 复核 ✅) | py_compile 全部通过 ✅
Next: 等用户确认 Agent 编排草案 (§8 的 7 项开放问题 + 三份草案 [agent-boundaries / safety-hard-rules / agent-communication-analysis]) 后开发编排层; 或继续把同类反序列化/健壮性加固推广到剩余未覆盖模块 (候选: scheduled_stories.py 故事缓存读路径、accounts.py 账户文件读路径 — 需先确认崩溃路径与可达端点), 或继续打磨交互式追问体验 (确认题/多选、追问后自动采纳的"真正自动"变体等形态)
Blockers:
- Agent 编排体系 — 需用户确认 plan.md §8 的 7 项开放问题 + 三份草案后方可进入开发 (plan 明确禁止 agent 自行启动)

Open questions:
- 是否采用 docs/agent-boundaries.md 中的 7 项推荐默认值？(回复「全部采用推荐默认值」即可; #1 通信方案的深入技术分析见 docs/agent-communication-analysis.md)
- 是否采用 docs/safety-hard-rules.md 中的 9 条安全硬规则？(回复「安全规则采用草案」即可)
- 一键全部采纳已就绪 (2026-08-19, 需点一次按钮); 是否需要升级为"追问后完全自动采纳"（不经点击直接加入计划）？涉及用户可见行为变化, 需确认

Updated: 2026-09-15
