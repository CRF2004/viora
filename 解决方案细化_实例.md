# PPT 解决方案细化：基于 Viora 的医疗多源暗数据激活实例

> 以下内容可替换或补充 PPT 第 6-8 页（问题识别/核心方法/治理支撑），提供一个完整的实例化方案。

---

## 一、解决方案总览：从零散聊天到可治理的数据资产

```
用户聊天消息（暗数据源头）
    │
    ▼
┌───────────── LLM 结构化抽取 ─────────────┐
│  自然语言 → 8 维健康结构化数据           │
│  (睡眠/精力/情绪/运动/肠胃/皮肤/疼痛/饮食)│
└─────────────────────────────────────────┘
    │
    ├──→ Routine 学习（作息模式建模）
    ├──→ 基线更新（滑动平均，EWMA 衰减 0.8）
    ├──→ 异常检测（偏差阈值判断）
    ├──→ 跨维度关联（Pearson 相关性计算）
    └──→ 图谱构建（节点 + 边 + 相关系数）
    │
    ▼
┌───────────── 主动智能触发 ─────────────┐
│  Routine 异常 → 主动关心               │
│  模式关联 → 洞察反馈                   │
│  环境变化 → 天气提醒                   │
│  长期沉默 → 轻量 Check-in              │
└─────────────────────────────────────────┘
    │
    ▼
┌───────────── 数据资产化输出 ─────────────┐
│  身体周报（周叙事生成）                  │
│  健康图谱（跨维度关联可视化）            │
│  健康计划（推理树 + 行动叶子节点）       │
│  趋势洞察（相关性 + 趋势判定）           │
└─────────────────────────────────────────┘
```

---

## 二、暗数据激活三条件的具体落地

### 条件 1：需求明确 — 能否服务健康状态理解？

**Viora 的实现：8 维健康结构抽取 + Persona 驱动**

每一条用户自然语言消息，经过 LLM 结构化抽取，转化为 8 个健康维度的标准化评分：

| 维度 | 评分标准 | 示例输入 → 结构化输出 |
|------|---------|---------------------|
| 睡眠 | quality 1-5 + duration_hours | "凌晨三点才睡着" → `{"quality": 1, "duration_hours": null}` |
| 精力 | score 1-5 + reason | "精神不错" → `{"score": 4, "reason": "睡得好精神饱满"}` |
| 情绪 | score 1-5 + reason | "心情很低落" → `{"score": 2, "reason": "没睡好心情低落"}` |
| 运动 | type + duration_minutes | "跑了五公里" → `{"type": "跑步", "duration_minutes": null}` |
| 肠胃 | status + severity 1-5 | "胃不舒服，胀气" → `{"status": "胃不适，胀气", "severity": 3}` |
| 皮肤 | status | "最近长痘" → `{"status": "长痘"}` |
| 疼痛 | location + severity 1-5 | "头疼" → `{"location": "头", "severity": 3}` |
| 饮食 | notes | "今天吃得清淡" → `{"notes": "吃得清淡"}` |

**关键设计**：不是通过问卷获取，而是从自然对话中悄无声息地提取。用户不需要改变行为，数据就在交互中被激活。

### 条件 2：价值增量 — 是否补充单一设备无法提供的信息？

**Viora 的实现：跨维度关联 + Routine 学习**

单一设备（手环、手表）能提供睡眠时长、心率、步数，但无法提供：

- **主观感受**：用户真实的情绪状态、精力感知、疼痛描述
- **隐性关联**："睡眠差的两天后胃一定不舒服" — 这种跨维度的模式发现需要长期积累和统计
- **生活情境**：用户在备考、在搬家、在加班 — 这些数据影响健康但设备无法捕获

Viora 的 Routine 学习引擎从消息历史中提取作息模式：

```python
# RoutineModel 自动学习的内容
- 睡眠基线：平均入睡时间、平均睡眠质量、工作日 vs 周末差异
- 运动习惯：运动类型分布、时间段偏好、频率
- 情绪周期：一周中情绪波动模式、与睡眠质量的关联
- 精力基线：与前一天睡眠质量的相关性
```

体检数据（exam_graph）更进一步，将结构化体检报告转化为图谱节点，与日常聊天数据形成互补：

| 数据源 | 时间粒度 | 覆盖维度 | Viora 如何整合 |
|--------|---------|---------|---------------|
| 设备监测 | 连续/分钟级 | 心率、运动、血氧 | Routine 基线对比 |
| 聊天表达 | 离散/事件驱动 | 情绪、精力、疼痛、肠胃 | 结构化提取（8维） |
| 体检报告 | 低频/年度 | 生化指标、影像 | 图谱节点可视化 |

### 条件 3：治理可行 — 是否能被授权、标准化、校验并持续更新？

**Viora 的实现：标准化 + 质量校验 + 增量更新**

**标准化**：所有数据统一为 JSON schema，字段名、取值范围、单位全部一致。

```json
// 标准化后的数据示例
{
  "sleep": {"quality": 3, "duration_hours": 6.5},
  "energy": {"score": 3, "reason": null},
  "mood": {"score": 4, "reason": "今天心情不错"},
  "exercise": {"type": "步行", "duration_minutes": 30}
}
```

**质量校验**：
- LLM 输出异常检测（捕获非 JSON、空对象等异常）
- 数值范围校验（评分 1-5，超出视为无效）
- 数据源追踪（每条消息带有 `source` 标记：主动/被动/定时提醒）
- 锁文件机制（fcntl.flock 保证写入原子性）
- 数据恢复（Corrupt JSON 自动备份到 `.corrupt` 文件，不会静默丢失）

**增量更新**：
- 用户状态使用 EWMA（指数加权移动平均）更新基线：`new_baseline = old × 0.8 + current × 0.2`
- 趋势判定：三次消息即可计算跨维度 Pearson 相关系数
- 故事缓存：按 `(user_id, period)` 缓存身体故事，新消息到达后自动失效

---

## 三、治理四目标的具体实现

| 治理目标 | Viora 实现 | 技术机制 |
|---------|-----------|---------|
| **可管理** | 统一 Schema + 标准化抽取 | LLM 固定 Prompt 输出 8 维 JSON，字段/范围/类型统一 |
| **可用可信** | 完整性校验 + 异常检测 | 非空校验、范围校验、Corrupt 文件自动备份 + 取证保留 |
| **可复用** | 图谱关联 + 洞察生成 | 跨维度 Pearson 相关系数，维度间关联自动发现和描述 |
| **可追溯问责** | 全链路追踪 | 每条数据有 `user_id + timestamp + message_id + is_proactive` 标记，可追溯至原始对话 |

### 3.1 可管理 — 标准治理

- 统一健康维度：8 个核心维度 + 无限扩展的 profile_notes
- 统一评分标尺：1-5 Likert 量表
- 统一时间格式：ISO 8601 UTC
- 统一用户标识：user_id UUID

### 3.2 可用可信 — 完整性治理、准确性治理

**完整性治理**：
- 即使部分维度缺失，不影响其他维度的可用性
- "沉默"作为有效状态记录（`__VIORA_SILENT__` 标记）
- 数据点不足时主动降级（"数据还不够多，但我能看到一点点苗头..."）

**准确性治理**：
- LLM 提取健康数据后，通过账户活跃度评分`compute_activity_score()` 交叉验证（活跃用户的数据置信度更高）
- 异常检测：Routine 偏差超过阈值则标记为 `anomaly_level: "high"`
- 趋势判定：需至少 3 条数据点才能计算相关性，不足时不产生虚假洞察

### 3.3 可复用 — 一致性治理、时效性治理

**一致性治理**：
- 不同维度的数据通过 `generate_health_graph()` 统一表示为图谱节点和边
- 体检数据（exam_graph）和聊天数据的维度标签统一映射
- 所有数据可汇总为 HealthPlan 推理树的叶子节点

**时效性治理**：
- 主动关心冷却机制：每日最多 1 次、12 小时冷却
- 用户沉默退让：连续 2 次不回应 → 自动降低交互频率
- 数据窗口：故事生成限定时间段（日/周/月）
- 趋势计算区分"近期"和"历史"：recent = 最新 3 条，earlier = 更早的

### 3.4 可追溯问责 — 可追溯治理、隐私合规

**可追溯治理**：
- 每条数据记录 `message_id + user_id + timestamp + is_proactive`
- 数据来源可追溯至原始对话消息（原始文本保留在 `raw_text` 字段）
- 状态变更全部带时间戳

**隐私合规**：
- 密码使用 `werkzeug.security.generate_password_hash` 哈希存储
- API 返回数据自动过滤敏感字段（`_public_account()` 移除 `password_hash`）
- 会话管理：基于 Flask session 的 `require_login` 装饰器
- 用户注销支持：`delete_account()` + `delete_messages_for_user()` 全量删除

---

## 四、完整实例推演：一个用户的一天

### 场景

用户小橙（27 岁，产品经理）早上醒来跟 Viora 发了一条消息。

### Step 1：暗数据产生

> 用户输入："昨晚又熬夜了，三点才睡，今天感觉好累。胃也有点不舒服。"

这条消息在传统框架中是"聊天记录"——有内容但无法直接分析，属于**暗数据**。

### Step 2：结构化激活

LLM 结构化抽取引擎将其转化为：

```json
{
  "sleep": {"quality": 1, "duration_hours": 4.0},
  "energy": {"score": 2, "reason": "熬夜导致精神差"},
  "mood": {"score": null, "reason": null},
  "exercise": {"type": null, "duration_minutes": null},
  "digestion": {"status": "胃不适", "severity": 3},
  "skin": {"status": null},
  "pain": {"location": null, "severity": null},
  "diet": {"notes": null}
}
```

暗数据 → 标准化结构数据 ✅

### Step 3：Routine 学习 & 异常检测

Routine 模型对比用户基线：
- 用户通常入睡时间：23:30-00:30
- 当前记录：03:00 → 偏差超过 2.5 小时
- 睡眠质量基线：3.2 → 当前 1 → 偏差超过阈值
- 肠胃基线：无异常记录 → 当前 severity 3 → 新问题标记

→ `anomaly_level: "high"` ✅

### Step 4：智能响应 + 数据更新

Viora 根据 Persona（温暖好友）和上下文生成回复：

> "😮 三点才睡？你这是又在赶需求了吧。
> 胃不舒服估计也是熬夜闹的，你之前不是说胃还行嘛。
> 今天尽量别喝咖啡了，让胃缓缓~"

同时后台完成：
- 睡眠基线更新：`baseline = 3.2 × 0.8 + 1 × 0.2 = 2.76`（下降标记）
- 肠胃基线新建：`severity = 3`
- 活跃度更新：`interaction_count + 1`
- 故事缓存失效：新数据到达，缓存标记为脏

### Step 5：跨维度关联发现

当数据积累到一定程度后（≥3 条同时有睡眠和肠胃记录），`compute_correlations()` 计算发现：

- 睡眠质量 vs 肠胃：`r = -0.62`（中等负相关）
- 描述："睡眠越差，肠胃也越差"

这个关联洞察在下一次身体周报中被讲述为：
> "悄悄说，我发现你睡不好的时候胃也容易闹脾气，这两个小家伙好像在玩跷跷板。"

**这组关联信息是单一设备（手环）无法提供的** ✅

### Step 6：持续治理闭环

| 治理环节 | 具体操作 |
|---------|---------|
| 标准 | "三点睡"→ sleep.quality=1, duration_hours=4.0（统一度量） |
| 完整性 | 即使情绪/运动/皮肤/饮食无数据，其余 4 维正常使用 |
| 准确性 | 异常标记为 high，后续交互中 AI 主动确认："胃好点了吗？" |
| 一致性 | 本次肠胃异常记录与体检报告的"慢性胃炎"历史图谱节点关联 |
| 时效性 | 下次主动关心安排在 12 小时后，不会立即追问 |
| 可追溯 | 这条数据可回溯至原始消息文本及时间戳 |

### Step 7：数据资产化输出

一周后，Viora 生成身体周报：

> **标题：⚡ 本周电量不太够**
> 
> **数据速览：**
> 🛏 平均睡眠 5.8 小时，比上周少了 1 小时
> 😊 情绪得分偏低（周二最低）
> 🏃 运动 2 天，保持得还行
> 🤢 肠胃不适 2 次，都出现在熬夜后
> 
> **本周故事：**
> 这一周你的身体像一部开启了省电模式的手机。有两天熬夜到凌晨，第二天精力明显掉下来了。不过有意思的是，每次睡不好的第二天，胃也会跟着不舒服——这两个小家伙好像在联手抗议。
> 
> **小贴士：**
> ✨ 周三那天你睡了 7.5 小时后精力很好，试试把睡觉时间提前到 12 点？
> ✨ 连续两天胃不舒服了，明天早餐喝点粥养养？

这份周报本身就是**数据资产化的产物**——零散的聊天记录被转化为有叙事结构、有跨维度洞察、可消费的健康内容。

---

## 五、PPT 建议改动方式

### 建议新增一页："具体实例：以 Viora 为例的数据激活全流程"

位置：在"核心方法"（当前第 7 页）之后、"治理支撑"（当前第 8 页）之前，或合并到第 7 页的右侧/下半部分。

### 建议在"核心方法"页增加实例映射

当前内容："暗数据激活的三条件：需求明确 + 价值增量 + 治理可行"

建议补充每一条件右侧的实例说明框，形成二分排版：

| 条件 | 宏观原则 | Viora 实例 |
|------|---------|-----------|
| 需求明确 | 能否服务健康状态理解 | LLM 从聊天中提取 8 维健康数据 |
| 价值增量 | 是否补充单一设备无法提供的信息 | 跨维度 Pearson 关联发现（如睡眠↔肠胃） |
| 治理可行 | 是否能被授权、标准化、校验并持续更新 | JSON Schema 标准化 + EWMA 增量更新 + Corrupt 自动备份 |

### 建议在"治理支撑"页增加"治理流水线"图示

```
用户消息 → LLM抽取 → Schema校验 → 基线更新 → Routine学习 → 图谱关联 → 资产输出
                                      ↓
                               Corrupt备份 / 范围校验 / 异常标记
```

---

*以上内容可直接用于 PPT 第 6-8 页的细化补充。如果需要我进一步对某个部分展开，或直接生成新的幻灯片内容，请告诉我。*
