# Viora 产品计划

## 1. 产品定位

### 一句话定位
**Viora：不是健康应用，是主动关心你的身体搭子。**

### 是什么，不是什么
| 是 | 不是 |
|---|---|
| 主动智能 — 在适当的时候主动找你，不等你来问 | 被动助手 — 不是你说一句它回一句的问答机器 |
| 熟人关系 — 长期记住你的习惯和偏好，不需要每次重新解释 | 陌生人客服 — 不是每次都像第一次对话的工具 |
| 朋友聊天 — 信息从自然对话中悄无声息地获取 | 数据收集器 — 不是"你今天运动了吗？"式的问卷打卡 |
| 有性格的 AI — 有语气、有温度、可自定义的 Persona | 冰冷的机器人 — 不是千篇一律的客服话术 |
| 身体故事讲述器 — 把数据变成可回看的叙事 | 数据仪表盘 — 不是表格和数字的堆砌 |
| 零压力的轻记录方式 — 不需要主动记录，AI 在聊天中就懂了 | 任务体系 — 没有 streak、没有"你已经 X 天没来了" |
| 基于图谱的健康洞察 — 发现身体各维度之间的关联 | 医疗诊断工具 — 不做处方、不建议用药 |

### 主动智能 vs 被动响应：根本差异

| 维度 | 被动响应式 AI | Viora 主动智能 |
|---|---|---|
| 谁发起对话 | 用户 | AI 也会在适当时机发起 |
| 信息获取方式 | 用户主动说 / 填表 | 朋友式聊天中自然获取 |
| 记忆 | 用户每次要重新交代上下文 | 长期记忆，像熟人一样记得你的习惯 |
| 交互节奏 | 用户控制 | AI 根据你的日常节奏主动介入 |
| 感受 | "我在用一个工具" | "我在和懂我的人聊天" |

### 市场差异化
当前市场上的健康工具都在等用户主动——等用户打开 app、等用户填表、等用户打卡。Viora 走了一条完全不同的路：

- **AI 主动找你**，不是等你来找 AI。像一个关心你的朋友，在你作息异常、天气变化、或者该运动的时候轻声说一句。
- **悄无声息地了解你**，不是问卷式的"你今天睡了几小时"，而是"看你今天起得挺晚的，昨晚应该睡好了吧？"——朋友式的关心，同时在获取信息。
- **摸清你的日常节奏**，然后在合适的时机用合适的方式介入，不做骚扰型的推送机器。

Apple Health 统治了数据层，但没有叙事层和对话层，更没有主动智能。
Flux（健康日记）理念最接近，但仍需每日填表，AI 不会主动找你。
BoDo（波度）是轻量打卡，但问卷疲劳是核心痛点。
MeYou Health 过于临床化，面向的是有明确疾病的人群。

Viora 的机会：**占据"主动式 AI 身体搭子"这个品类** — AI 不只是回答你的问题，而是在适当的时候主动关心你，像一个真正关注你的朋友。

---

## 2. 目标用户画像

### 核心用户：都市青年（20-35 岁）

**画像：小橙，27 岁，上海，产品经理**

- 70%+ 的中国城市青年处于"亚健康"状态，小橙就是其中之一
- 体检报告有 2-3 个箭头（偏高的指标），但不算病，去医院又觉得不至于
- 偶尔胃不舒服、熬夜后第二天没精神、情绪波动 — 这些体验很常见，但没人帮她串起来看
- 她不想被"健康管理"绑住 — 下载了健康类 app，用了三天就搁置了
- 她用微信发语音多于打字 — 语音是最自然的沟通方式
- **她不希望有个机器人每天问她"今天运动了吗"——她希望有个能主动关心她、又不会让她觉得被打扰的人**
- **她不需要每次跟一个新工具重新解释自己。她想要一个记得她习惯、懂她节奏的"熟人"**
- 她会在朋友圈发"今天好累"，但需要一个更安全的地方记录身体感受

**用户需求层次：**

| 层次 | 需求 | Viora 的回应 |
|---|---|---|
| 表层 | 有个地方随手记录身体感受 | 聊天框即入口，AI 在对话中自然提取 |
| 中层 | 有人主动关心我的状态 | AI 在适当时机主动找我，不是等我来问 |
| 中层 | 想回看自己最近的状态 | 身体故事时间线 |
| 深层 | 想知道"为什么我最近总是这样" | 图谱关联 + AI 趋势洞察 |
| 深层 | 不想被评判、不想有压力 | 零 gamification + 朋友式语气 |
| 底层 | 想要一个"熟人"，不是每次重新解释的陌生人 | 长期记忆 + 可自定义 Persona |

### 使用场景
- 某天早上，Viora 主动发消息："你今天应该去跑步了吧，但一直没动静，是计划改了吗？"
- 晚上躺在床上，跟 Viora 随口聊了几句今天的感受——像跟朋友发微信一样自然
- Viora 发现她连续三天睡得晚，主动问："你这两天睡得都好晚，是不是有什么事？"
- 周末回看本周身体故事 — "原来每次熬夜后第二天精力都低"
- 发现关联："你总在睡眠差的两天后胃不舒服"
- 天气降温，Viora 提醒："今天降温了，你之前说容易手脚冰凉，记得多穿点"
- 转发一条 WeChat 消息给 Viora（未来），系统自动提取其中的健康线索

---

## 3. 核心理念

### 理念 1：主动智能，不是被动响应
**Viora 不是等用户来问的工具，它会在适当的时候主动发起交互。**
- 像一个真正关注你的朋友，而不是一个沉默的问答机器
- 发现你的作息异常时提醒你，天气变化时关心你，你连续几天状态不好时主动问
- 但不过度 — 主动不等于骚扰，有分寸感
- 核心理念：AI 不只是"你问我答"，而是"我该找你的时候找你"

### 理念 2：熟人关系，不是陌生人
**Viora 不是每次对话都要重新解释的客服，是长期记忆、长期关系的熟人。**
- 记得你的偏好、你的习惯、你之前说过的话
- 用户不会觉得"我在记录健康数据"，而是"我在和懂我的人聊天"
- 不需要每次重新建立上下文——"你不是记得我胃不好吗？"
- 核心理念：不是工具，是朋友

### 理念 3：摸清用户的 daily routine
**Viora 的核心能力是学习并理解用户的日常作息模式。**
- 它知道用户几点起床、什么时候上班、午饭大概吃什么、晚上几点运动、什么时候睡觉
- 不是通过"问卷"获取这些信息，而是在长期交互中逐渐"摸清"的
- 基于 routine，它知道什么时候找你最合适——不是半夜三点，也不是你开会的时候
- 核心理念：理解你的节奏，才能在你需要的时候出现

### 理念 4：悄无声息的信息搜集
**Viora 不是在交互中"收集数据"的。它看起来就是朋友之间的日常聊天。**

| 搜集信息式（❌） | 朋友式（✅） |
|---|---|
| "你今天运动了吗？" | "今天天气这么好，你上次不是说想多走走吗？" |
| "昨晚睡了几个小时？" | "看你早上起的挺晚的，昨晚应该是睡好了吧？精神还行吗？" |
| "今天吃了什么？" | "周末有没有好好吃顿饭？别又随便对付了" |

用户不会觉得自己在"打卡"或"记录"，但 Viora 从这些对话中自然提取了健康数据。

### 理念 5：AI 有 Persona
**Viora 本身有性格、有温度、有说话方式——像一个真实的人。**
- 比如可能有点毒舌但关心你、或者温柔但不唠叨
- 用户可以自定义这个角色——"像个健身教练"、"像个中医阿姨"、"像个毒舌闺蜜"
- Persona 不只是语气风格，还包括：关心什么、怎么表达关心、什么时候该管你什么时候不该管你
- 核心理念：不是一个没有面目的 AI，是一个有血有肉的"人"

### 理念 6：身体故事 + 图谱叙事
**数据是原料，故事是产品。**
- 用户看到的不是"心率 72、睡眠 6.5h、步数 6000"，而是"这三天你像一部省电模式运行的手机"
- 时间线叙事：按时间轴串联记录，形成可读的"身体故事"
- 图谱关联：让你看到身体各维度之间的关系
- 可分享的叙事（未来）：用户可以导出自己的某段身体故事

### 理念 7：零压力
**永远不展示"距上次记录 X 天"，永远不暗示用户"该记录了"。**
- 没有 streak、没有徽章、没有积分
- 用户可以三天不说话、然后一天聊五条 — 系统不会有任何评判
- AI 主动找你，但不会因为你没回应就追加催促
- 设计理念：用户是自由的，Viora 是一个有分寸的朋友，不是监工

### 理念 8：时间感知
**AI 必须知道"现在是什么时候"，才能给出合理的回复。**

- 问题场景：AI 建议用户"今晚十一点睡觉"，但对话实际发生在 23:42，建议完全不合理。
- AI 在生成回复时，必须在系统 prompt 中注入当前时间信息（日期、星期、精确到小时的时间段）。
- 时间段判断（凌晨 / 上午 / 中午 / 下午 / 傍晚 / 深夜），让 AI 能做出与时间匹配的回复。
- 智能关心：如果当前时间晚于用户通常的睡觉时间，AI 用关心的语气而不是"该睡了"的说教。
- Phase 1 即可实现，只需在每次 API 调用的 system prompt 中加入时间上下文。

### 理念 9：环境感知（天气、温度）
**AI 能结合天气和温度主动关心用户，像一个注意到天气变化的朋友。**

- 例如："今天降温了，你之前说容易手脚冰凉，记得多穿点"。
- 用户授权获取地理位置（经纬度或城市名），或手动设置所在城市。
- 接入免费天气 API（如和风天气、OpenWeatherMap），在 AI 生成回复时将天气作为上下文注入。
- 不需要实时天气推送，只在 AI 生成回复时作为系统 prompt 的一部分。
- Phase 2/3 实现，依赖用户位置授权和天气 API 集成。

---

## 4. AI Persona 系统设计

### 默认 Persona
Viora 的默认角色方向：
- **关心你但不唠叨** — 会主动找你，但点到为止，不会连环追问
- **有点小幽默** — 不是冷冰冰的"检测到您的睡眠不足"，而是"你这两天睡得比猫还少啊"
- **熟人语气** — 不是"尊敬的用户"，是"你今天怎么了"
- **有判断力** — 知道什么时候该关心你、什么时候不该打扰你

### 可自定义 Persona
用户可以描述自己想要的角色特征，例如：
- "像个健身教练" — 语气硬朗，关心运动量，会"逼"你动起来
- "像个中医阿姨" — 温柔唠叨，关心饮食和作息，提醒你"别吃凉的"
- "像个毒舌闺蜜" — 说话直接有点损但出发点都是关心你
- "像个理性分析师" — 冷静、用数据说话、不感情用事

### Persona 维度
Persona 不是一个标签，而是一个多维度的配置：

| 维度 | 说明 | 示例 |
|---|---|---|
| 语气风格 | AI 说话的方式 | 温柔 / 毒舌 / 幽默 / 正经 / 随意 |
| 关心重点 | AI 最关注什么 | 运动 / 睡眠 / 饮食 / 情绪 / 全面关心 |
| 互动频率 | 多久主动找你一次 | 高频（每天）/ 中频（隔天）/ 低频（有问题才找） |
| "管你"的程度 | AI 对你有多"管" | 随便说说 / 适度提醒 / 严格要求 |
| 称呼方式 | AI 怎么叫用户 | 名字 / 昵称 / 不称呼 |

### Persona 不只是装饰
Persona 决定了：
- **什么时候找你** — 健身教练 Persona 会在你运动时间到了的时候找你；中医阿姨 Persona 会在饭点提醒你吃饭
- **怎么找你** — 毒舌闺蜜用调侃的方式，温柔型用关心的语气
- **说什么** — 不同的 Persona 关注的维度不同，关心的方式不同
- **你不想聊的时候怎么退** — 有些 Persona 会追问，有些会体贴地撤回

---

## 5. 主动交互机制设计

### 触发条件

Viora 会在以下情况主动找你：

| 触发类型 | 场景示例 | 交互方式 |
|---|---|---|
| Routine 异常 | "你今天应该去跑步了吧，但一直没动静" | 轻量提醒，不是质问 |
| 模式关联 | "你这两天睡得都好晚，是不是有什么事？" | 关心式询问 |
| 关怀式介入 | "记得你之前说胃不舒服，今天感觉怎么样？" | 自然追问，像朋友一样 |
| 环境触发 | "今天降温了，你之前说容易手脚冰凉，记得多穿点" | 基于天气、节日等外部信号 |
| 正向反馈 | "这周你运动了三次，比之前多了一倍" | 分享好消息，不是夸奖式施压 |

### 交互方式
- **不是问答题** — 不用"你今天运动了吗"这种搜集信息式的问法
- **是朋友聊天** — "今天天气这么好，你上次不是说想多走走吗？"
- **不是打卡提醒** — 不用"你三天没记录了"这种施压式推送
- **是关心式介入** — "感觉你最近状态不太对，没事吧？"

### 不骚扰的边界
主动关心 ≠ 无节制推送。Viora 的边界：
- **频率控制** — 每天主动找你不超过一次（除非检测到明显异常）
- **场景感知** — 不在你通常忙碌的时间段找你（比如工作日的上午）
- **用户偏好** — 如果用户连续两次没回应，下次会降低主动频率
- **沉默模式** — 用户可以一键开启"别找我"模式，在设定时间内完全不打扰
- **退让机制** — 如果用户说"现在不想聊"，AI 会得体地结束对话，不会追问"为什么"

### 5.6 时间感知系统

AI 在每次生成回复时，系统自动在 system prompt 中注入当前时间上下文：

| 注入字段 | 示例 | 说明 |
|---|---|---|
| 当前时间 | `2025-05-08 23:42` | 精确到分钟 |
| 星期 | `星期四` | 用于判断工作日/周末 |
| 时间段 | `深夜` | 凌晨（0-5点）、上午（5-12点）、中午（12-14点）、下午（14-18点）、傍晚（18-20点）、深夜（20-24点） |
| contextual\_hint | `当前时间晚于用户通常睡觉时间` | AI 据此调整语气——用关心而非建议 |

**实现方式：**
- 后端在每次调用 LLM API 前，读取服务器当前时间，生成上述字段。
- 字段拼接到 system prompt 中，对用户不可见。
- Phase 1 即可实现，无需额外依赖。

### 5.7 环境感知系统（天气/温度）

AI 结合天气和温度信息生成关心式消息，像朋友注意到天气变化时自然提醒。

| 组件 | 说明 |
|---|---|
| 位置获取 | 用户授权获取经纬度或城市名，或手动设置所在城市 |
| 天气 API | 接入免费天气 API（如和风天气、OpenWeatherMap） |
| 数据缓存 | 天气数据不实时刷新，每次 AI 生成回复前查询一次（或缓存最近 30 分钟数据） |
| 注入字段 | `当前天气：{weather}`、`温度：{temperature}`、`空气质量：{aqi}`（如果可用） |

**交互示例：**
- "今天降温了，你之前说容易手脚冰凉，记得多穿点"
- "这几天一直下雨，你平时说的膝关节不舒服有没有加重？"
- "今天空气质量不太好，出门记得戴口罩"

**Phase 2/3 实现：** 依赖用户位置授权和天气 API 集成。不需要实时推送，只在 AI 生成回复时作为上下文注入。

---

### 5.8 对话交互体验优化方案（v1.0）

> 目标：显著提升聊天自然度与“像真人朋友”的感觉，解决当前单纯依赖 Prompt 导致回复风格不稳定、单条长回复、缺少碎片化与情绪流动的问题。

#### 5.8.1 核心问题
- 纯 Prompt 难以精准控制回复的**情感意图**（同情、分析、建议、幽默等）。
- 回复形式过于“AI 化”：单条、完整长句、缺少语气词和 emoji。
- 真实朋友聊天是**碎片化、连发、多表情**的自然流动，目前产品未能还原这一点。

#### 5.8.2 优化目标
- 实现**意图可控**的智能回复决策。
- 支持**多短消息连发（Message Burst）** + 自然 emoji + 口语化表达。
- 保持 Viora “主动关心、有性格、零压力”的核心理念。
- 方案保持轻量，优先规则驱动，后续可逐步迭代，不引入重型框架。

#### 5.8.3 推荐交互生成流程
每次用户消息或主动触发后，按以下 4 层处理：

1. **感知层（Perception）**
   - 提取当前输入信号、用户状态、历史上下文。

2. **决策层（Decision / Response Planner）** ← 新增核心模块
   - 决定本次回复的**主要意图**、**语气强度**、**消息数量**、**emoji 密度**。

3. **生成层（Generation）**
   - 将决策结果注入结构化 Prompt，让 LLM 只负责自然语言表达。

4. **后处理层**
   - 拆分多条消息
   - 提取结构化健康数据
   - 更新用户状态
   - 返回给前端（支持逐条显示）

#### 5.8.4 关键数据模型

**User State（用户状态）**：建议在 `storage.py` 或新增 `user_state.py` 中维护（JSON + SQLite）
- 生理/作息基线（睡眠、精力、情绪等均值与趋势）
- 当前偏差 / 异常等级（low / medium / high）
- 最近情绪倾向
- 互动亲密度（`interaction_count`、`avg_reply_length`）
- 用户偏好（不喜欢过度建议等）
- Routine 模型摘要（可复用 `routine.py`）

**Persona 规则表（persona.py 增强）**：将现有 Persona 转化为可配置规则

| Persona | 共情 | 分析 | 建议 | 幽默/毒舌 | 主动关心强度 | 多消息概率 | Emoji 密度 |
|---|---|---|---|---|---|---|---|
| 温暖好友 | 高 | 中 | 低 | 中 | 低 | 65% | 中 |
| 健身教练 | 低 | 高 | 高 | 中 | 中 | 40% | 中低 |
| 中医阿姨 | 中 | 高 | 高 | 低 | 中 | 55% | 低 |
| 毒舌闺蜜 | 低 | 中 | 中 | 高 | 低 | 75% | 高 |
| 理性分析师 | 低 | 高 | 中 | 低 | 高 | 30% | 低 |

每个 Persona 增加：
- **禁止事项**
- **语气词偏好**

#### 5.8.5 Response Planner（核心新增模块）
建议在 `ai_engine.py` 中新增 `ResponsePlanner` 类，包含两个主要方法：
- `decide_intent(...)`：决定 `primary_intent`（empathy / analysis / advice / humor / light_checkin / question 等）
- `decide_burst(...)`：决定消息数量、风格、emoji 密度

**决策逻辑示例**：
- 高异常 + 用户疲惫 → 优先 empathy + 多消息
- 用户主动分享 → analysis + humor（依 Persona）
- 主动关心 → light_checkin + 低强度 + 短消息连发

#### 5.8.6 Prompt 模板
```text
你是[Viora 当前 Persona 详细描述]，正在和熟悉的朋友自然聊天。

本次回复决策：
- 主要意图：{primary_intent}，语气强度：{intensity}
- 消息数量：{num_messages} 条
- Emoji 密度：{emoji_density}（轻/中/高）
- 约束：{persona_constraints}

用户当前状态摘要：{brief_user_state}
最近关联洞察：{insights}

用户最新消息：{user_input}

回复要求（严格遵守）：
- 每条消息 4-25 字左右，口语化
- 使用语气词（嗯、哎、哈哈、欸、啧、～）
- 可以连发，中间自然停顿
- 适当使用 emoji
- 不要一次性说完，留空间给对方回复
- 像真实朋友聊天，不要像客服或医生

请直接输出 {num_messages} 条消息，用 --- 分隔：
消息1
---
消息2
...
```

#### 5.8.7 前端配合要求（`index.html`）
- AI 回复返回 `messages: ["消息1", "消息2", ...]` 数组
- 前端逐条显示，每条之间增加随机延迟（300-800ms）
- 显示“正在输入...”动画
- 支持连续气泡样式（多个小气泡连在一起）
- 用户端支持自然连发消息（不强制合并）

#### 5.8.8 主动关心场景特殊处理
- 主动触发时优先使用**轻关心 + 多短消息 + emoji** 开场
- 示例：
  - 欸～
  - 今天天气这么好，你不会又宅着吧😏
  - 上次说想多走走，还记得不？

#### 5.8.9 实施优先级建议（短期可落地）

**Phase A（最高优先，1-2 周）**
1. 实现 `ResponsePlanner`（先纯规则版，后可加小模型辅助）
2. 修改聊天生成 Prompt + Burst 逻辑
3. 后端返回消息数组，前端逐条显示
4. 更新 Persona 配置表

**Phase B**
- User State 模型完善与持久化
- Emoji 后处理增强函数
- 对话反馈机制（可选拇指）
- 收集真实微信聊天记录做 few-shot 示例

**Phase C**
- 多 Agent 轻实现（Extractor + Planner + Generator）
- Behavior Tree 管理长期对话流

#### 5.8.10 预期效果
- 回复更碎片化、自然、带情绪
- 不同 Persona 性格差异更明显且稳定
- 用户感觉“这个 AI 真的像朋友在聊天”
- 便于后续迭代和 A/B 测试不同决策策略

---

## 6. Routine 学习系统

### 日常作息建模
Viora 在后台默默建模用户的日常作息：

| 维度 | 建模内容 |
|---|---|
| 起床时间 | 工作日 vs 周末的不同节奏 |
| 通勤方式 | 走路、骑车、地铁——也是运动量的一部分 |
| 工作节奏 | 上午专注 / 下午犯困 / 加班频率 |
| 用餐习惯 | 三餐时间、外卖 vs 做饭、饮食偏好 |
| 运动习惯 | 运动类型、频率、时间段 |
| 睡前习惯 | 刷手机、看书、运动——影响入睡时间 |
| 睡眠质量 | 入睡时间、醒来时间、睡眠规律性 |

### 学习过程：从陌生到熟悉

```
冷启动阶段（第 1-2 周）
  → 通过日常聊天初步了解用户作息
  → "你今天几点起的？" → "九点" → 记录：工作日大约 9 点起床
  → 主动交互较少，以观察和学习为主

熟悉阶段（第 3-4 周）
  → Routine 逐渐清晰
  → AI 开始基于 routine 做简单提醒
  → "你今天还没去跑步哦，计划改了吗？"

稳定阶段（1 个月后）
  → Routine 模型准确度持续提升
  → 异常检测开始工作
  → 模式关联（"你每周三下午特别容易犯困"）
  → 主动交互更精准、更自然
```

### 异常检测
当 Viora 发现你的行为偏离了正常模式时：
- "你今天起床比平时晚了一个小时，没事吧？"
- "你平时这个点应该在跑步了，今天换计划了？"
- "这周你好像都没怎么运动，是太忙了吗？"

异常检测不是为了"监控"你，而是为了在你状态变化的时候，能第一个关心你。

### 基于 Routine 的主动触发
Viora 的主动交互建立在对 routine 的理解上：
- 知道你通常 7 点跑步，7:15 还没动静时轻轻问一句
- 知道你晚上 11 点半通常睡觉，12 点半了还没睡 → "还不睡吗？"
- 知道你周五晚上通常出去吃饭 → 周末主动问"周末有什么安排？"
- 知道你工作日午餐通常点外卖 → 偶尔提醒"今天吃点好的，别又对付了"

### 图谱可视化承载层：事件 / 状态 / 轨迹 / Personality
Viora 的图谱可视化不只展示健康指标之间的关系，还要承载用户理解的多层抽象：

| 层级 | 图谱内容 | 典型节点 | 典型用途 |
|---|---|---|---|
| 事件层 | 从聊天记录中抽取的具体事件 | `周四 14:00 困得不想上课`、`晚上 1 点睡不着` | 还原原始语境、支持回看 |
| 状态层 | 一段时间窗口内聚合出的短期状态 | `近期睡眠不足`、`下午易犯困`、`情绪偏低` | 支持主动关心触发 |
| 轨迹层 | 更长时间尺度上的稳定模式 | `周四下午低能量`、`熬夜后次日精力低` | 发现可重复的日程轨迹 |
| Personality 层 | 长期稳定的倾向与表达风格 | `易焦虑`、`偏内倾`、`压力下回避任务` | 调整回复风格与关心策略 |

#### 视觉表现建议
- **同一张图谱，多层切换**：事件 / 状态 / 轨迹 / personality 共享同一视觉框架，通过层级切换查看不同抽象层。
- **时间轴 + 图谱结合**：支持时间滑块，查看某个事件如何累积成状态、再沉淀为轨迹。
- **节点颜色区分语义**：事件、状态、轨迹、人格节点使用不同颜色/形状。
- **点击下钻**：点击状态节点可回溯到原始聊天事件，点击轨迹节点可查看贡献事件列表。
- **置信度展示**：所有状态和 personality 推断都显示置信度、样本数、最近更新时间，避免“过度定性”。

#### 和健康图谱的关系
- 这套层级图谱可以**直接并入现有健康图谱可视化**，不单独再做一套入口。
- 健康维度关联图谱负责“维度之间的关系”，事件/状态/轨迹图谱负责“时间上的演化”。
- 前端可以在同一个图谱页面增加：
  - 层级切换（事件 / 状态 / 轨迹 / personality）
  - 时间范围切换（日 / 周 / 月）
  - 事件来源筛选（聊天、体检、天气、手动记录）

#### 数据结构设计：事件 / 状态 / 轨迹 / Personality

建议把图谱节点统一抽象成一套可扩展的 `GraphNode`，边统一抽象成 `GraphEdge`。这样前端图谱、时间线、主动关心引擎可以复用同一份数据结构。

**1）节点类型**

| 类型 | 作用 | 生成来源 | 关键字段 |
|---|---|---|---|
| `event` | 单条具体事件 | 聊天、体检、天气、手动记录 | 时间、来源、原始文本、抽取结果、情绪/身体标签 |
| `state` | 短期状态聚合 | 多个 event 在时间窗内聚合 | 时间窗、状态名、强度、置信度、样本数 |
| `trajectory` | 长期轨迹模式 | 多个 state 再聚合 / 模式识别 | 周期性、触发时段、持续时间、稳定度 |
| `personality` | 长期稳定倾向 | 多个 trajectory 统计沉淀 | 倾向标签、权重、置信度、更新时间 |

**2）统一节点结构（建议）**

```json
{
  "id": "event_20260515_001",
  "type": "event",
  "title": "周四 14:00 困得不想上课",
  "summary": "用户在周四下午表达明显困倦和上课回避",
  "timestamp": "2026-05-15T14:00:00+08:00",
  "source": "chat",
  "source_id": "msg_123",
  "tags": ["困倦", "课程回避", "低能量"],
  "confidence": 0.86,
  "importance": 0.72,
  "metadata": {
    "raw_text": "好困不想上课",
    "weekday": 4,
    "hour": 14,
    "persona_id": "warm_friend"
  }
}
```

**3）状态节点结构**

```json
{
  "id": "state_sleep_debt_7d",
  "type": "state",
  "title": "近期睡眠不足",
  "window": "7d",
  "summary": "过去 7 天多次出现晚睡、困倦、精力偏低",
  "confidence": 0.79,
  "strength": 0.63,
  "sample_count": 5,
  "updated_at": "2026-05-15T18:00:00+08:00",
  "evidence": ["event_1", "event_2", "event_3"]
}
```

**4）轨迹节点结构**

```json
{
  "id": "trajectory_thu_afternoon_low_energy",
  "type": "trajectory",
  "title": "周四下午低能量",
  "pattern": "weekly",
  "summary": "用户在周四下午更容易出现困倦、拖延、注意力下降",
  "period": "14:00-17:00",
  "confidence": 0.74,
  "stability": 0.68,
  "sample_count": 8,
  "updated_at": "2026-05-15T18:00:00+08:00",
  "evidence": ["state_1", "state_2"]
}
```

**5）Personality 节点结构**

```json
{
  "id": "personality_stress_avoidant",
  "type": "personality",
  "title": "压力下回避任务",
  "summary": "在压力上升时更倾向于拖延、回避沟通或先休息",
  "traits": ["回避型", "压力敏感", "内倾倾向"],
  "confidence": 0.61,
  "stability": 0.83,
  "sample_count": 24,
  "updated_at": "2026-05-15T18:00:00+08:00",
  "evidence": ["trajectory_1", "trajectory_2"]
}
```

**6）边结构**

| 边类型 | 含义 | 典型方向 |
|---|---|---|
| `causes` | 事件/状态之间的因果或关联 | `event -> state` |
| `aggregates_to` | 多个低层节点聚合成高层节点 | `event -> state`、`state -> trajectory` |
| `reinforces` | 节点之间相互强化 | `state -> state`、`trajectory -> personality` |
| `contrasts_with` | 相反模式 | `trajectory -> trajectory` |
| `evidence_of` | 证据指向上层概念 | `event -> trajectory`、`trajectory -> personality` |

统一边结构建议：

```json
{
  "id": "edge_001",
  "source": "event_20260515_001",
  "target": "state_sleep_debt_7d",
  "type": "aggregates_to",
  "weight": 0.82,
  "confidence": 0.86,
  "reason": "该事件被归类为困倦事件，且与近期睡眠不足状态一致",
  "updated_at": "2026-05-15T18:00:00+08:00"
}
```

**7）推荐存储方式**
- **JSON 作为 MVP 主存储**：方便快速调试和图谱展示。
- **SQLite 作为索引层**：用于按时间、类型、标签快速检索。
- **事件增量写入、状态定期重算**：事件实时落库；状态/轨迹/personality 可按消息数或时间窗重算。
- **保留原始证据链**：任何高层节点都要能回溯到具体 event，避免“黑箱画像”。

**8）更新策略**
- 新消息进入后先生成 `event`
- 再将相关事件写入对应时间窗，更新 `state`
- 定时或在样本足够时重算 `trajectory`
- personality 只做低频更新，避免一次情绪波动就改写长期画像

**9）可视化映射**
- `event`：小节点，密集时间分布
- `state`：中节点，作为聚合中心
- `trajectory`：大节点，显示周期标签（daily / weekly / monthly）
- `personality`：最少量节点，固定在画像层或侧边栏

这套结构可以直接接到现有图谱页面：左侧时间轴、中央关系图、右侧详情面板，下钻后看到证据链和时间分布。



### Phase 1：基础聊天 + AI 提取 + 初始 Persona + 简单时间线

**目标：跑通"用户说一句话 → AI 提取结构化数据 → 生成时间线故事"的核心闭环，同时建立基础 Persona**

| 功能 | 描述 | 验收标准 |
|---|---|---|
| 聊天式文本输入 | 用户在聊天框输入自然语言，如"今天走了很多路但睡得不好" | 能接收自由文本消息 |
| AI 自动提取 | AI 从自然语言中提取：睡眠、精力、情绪、运动、肠胃等维度的结构化数据 | 提取准确率 > 80%（手动测试） |
| 初始 Persona | 默认 Persona（关心但不唠叨、有点小幽默的熟人朋友）| 用户能感受到 AI 的语气和性格 |
| 结构化存储 | 提取的数据按时间存储在本地数据库 | 数据可查询、可追溯 |
| 身体故事时间线 | 以叙事形式展示历史记录："这周你的关键词是……" | 时间线可按周/月切换 |
| 体检数据导入 | 导入 JSON 格式的体检数据，作为健康图谱的骨架 | 能解析预设格式的体检 JSON |
| 基础图谱展示 | D3.js 渲染健康维度关系图 | 节点可点击、拖拽、缩放 |
| AI 时间感知 | 在 system prompt 中注入当前时间（日期、星期、时间段），让 AI 生成与时间匹配的回复 | AI 不会在深夜建议"该睡了"、在凌晨建议"早上好" |
| UI 优化 | 优化消息气泡排版（Markdown 换行、段落间距）、改进打字动画流畅度、微调配色方案、修复折叠面板动画、完善移动端响应式布局 | 长段落可读、移动端体验完整、动画过渡流畅 |

**不做：**
- 主动交互（Phase 2）
- Routine 学习（Phase 2）
- 语音输入
- 图谱关联计算
- AI 趋势洞察
- 用户账户系统（本地 demo 阶段）
- 天气/温度感知（Phase 2）

**研发周期预估：** 2-3 周

---

### Phase 2：Routine 学习 + 主动交互（轻量） + 图谱关联 + Persona 自定义

**目标：让 Viora 从被动聊天变成主动关心，开始学习用户节奏并在适当时机介入**

**状态：✅ 功能完成**

| 功能 | 描述 | 验收标准 |
|---|---|---|
| Routine 学习系统 | 从交互中逐步建模用户日常作息（起床、通勤、用餐、运动、睡前等） | ✅ 2 周后能识别 5+ 个日常维度 |
| 主动交互（轻量） | 基于 routine 异常触发主动消息 | ✅ 用户不觉得被骚扰 |
| 异常检测 | 检测行为偏离正常模式的情况 | ✅ 准确率 > 70%（人工标注验证） |
| 图谱关联计算 | 系统自动计算健康维度之间的关联强度 | ✅ 关联矩阵可计算、可展示 |
| 时间演化图谱 | 图谱支持时间滑块，用户可以看到关联关系随时间的变化 | ✅ 支持日/周/月切换 |
| AI 趋势洞察 | 系统生成描述性洞察 | ✅ 洞察基于真实数据，附置信度 |
| 身体故事增强 | 叙事支持关联引用 | ✅ 故事包含关联洞察 |
| Persona 自定义 | 用户可描述/选择想要的 AI 角色 | ✅ 5 种可选 Persona |
| 天气/温度感知 | 用户授权位置 + 天气 API 接入，AI 生成回复时注入天气、温度、空气质量等环境上下文 | ✅ AI 能结合天气信息生成关心式消息 |

**不做：**
- 社交分享
- 处方性建议
- 多模态输入（图片）
- 多渠道输入

**研发周期预估：** 4-5 周

---

### Phase 3：深度理解 + 多渠道 + 故事分享

**目标：打通多渠道输入、支持身体故事分享，让 Viora 成为用户生活中的健康叙事伙伴**

**状态：🔜 Phase 3-A 进行中 — ResponsePlanner + Message Burst**

#### Phase 3-A：ResponsePlanner + Message Burst（当前）

基于 plan.md 第 5.8 节交互优化方案，实现核心体验升级。

- [x] **persona.py — 补充 Persona 规则表**：每个 Persona 已有 empathy/analysis/advice/humor/burst_probability/emoji_density/forbidden/phrase_preferences 字段
- [x] **ai_engine.py — ResponsePlanner 类**：已有 `decide_intent()`（12 种 rich intent）、`decide_burst()`（按意图 + Persona 决定消息数量和 emoji 密度）、`decide_delay()`、`decide_opening()`、`decide_strategy()`
- [x] **ai_engine.py — Prompt 注入**：planner payload（primary_intent、num_messages、emoji_density、intensity、scenario）已注入 chat user prompt
- [x] **后端 chat API — 返回 messages 数组**：`/api/chat` 已返回 `{"messages": ["msg1", "msg2", ...]}` + 兼容旧 `"response"` 字段
- [x] **前端 index.html — 逐条显示 + 输入动画**：已有 `.message-burst` CSS、`appendMessageBubble()` 逐条渲染、sendMessage 逻辑
- [x] **测试覆盖 ResponsePlanner 决策逻辑**：30 个测试覆盖 `decide_intent()`（12 种意图分类）、`decide_burst()`（所有意图 + Persona 组合）、`decide_delay()`、`decide_opening()`、`decide_strategy()`、空数据边界情况

| 功能 | 描述 | 验收标准 |
|---|---|---|
| WeChat 转发提取 | 用户转发 WeChat 消息给 Viora（通过 WeChat bot），系统自动提取健康线索 | 能从对话/朋友圈/群聊消息中提取 |
| 语音记录 | 用户点击麦克风按钮，录制一段语音，系统自动转录并提取 | 转录准确率 > 85%（普通话） |
| 身体故事分享 | 用户可以将一段身体故事导出为图片/链接分享 | 分享格式美观、可读 |
| 多模态输入 | 支持图片输入（如拍照记录餐食、表情） | 图片可存储、可关联到时间线 |
| 多体检对比 | 支持多次体检报告对比，图谱中叠加时间维度 | 可直观看到指标变化 |
| 多渠道主动交互 | 主动消息可通过多种渠道触达用户 | 至少支持 2 种推送渠道 |
| 深度 Routine 理解 | 从简单作息建模进阶到模式识别和趋势预测 | 能发现 3+ 种跨维度关联模式 |

**研发周期预估：** 4-6 周

**待优化方向：**

- **松散多气泡交互：** 当前交互模型为「用户发一条 → AI 回一条」的同步问答模式。需要进一步优化为更松散的交互节奏——单次交互允许用户连续发送多个消息气泡（无需等待 AI 逐条回复），AI 可以延迟回复或不回复，类似真人聊天中的「想到哪说到哪」。具体细节（如消息合并策略、回复触发时机、多气泡 UI 表现、未回复消息的状态管理）需后续深入讨论设计。

---

### Phase 4：健康计划系统

**目标：让 Viora 从被动记录+主动关心的基础上，进化为能制定个性化健康计划、通过对话持续跟踪和调整计划的"健康搭子"**

**状态：🔧 持续完善中**（基本功能已实现，交互效果和推理树体验仍需迭代）

| 功能 | 描述 | 验收标准 |
|---|---|---|
| 计划生成 | AI 根据用户主诉，结合作息(Routine)、画像(Persona)、背景(Profile Notes)生成结构化健康计划 | 计划包含 3+ 类别，每类 2+ 条可执行建议 |
| 计划页面 | 独立页面展示当前计划，按类别和时间线组织，支持管理历史计划 | 清晰易读，支持查看/切换/归档历史计划 |
| 计划提醒 Agent | 用户可通过对话创建定时提醒（"每天下午三点提醒我跑步"），AI 在适当时机弹性执行 | 提醒触发时在 chat 中推送个性化消息，且会判断用户状态决定是否真的发送 |
| 计划上下文注入 | 每次 chat 回复时自动参考当前计划内容，让对话与计划自然联动 | AI 回复关联计划内容不生硬，如"你计划里今天有跑步，累了就休息" |
| 计划执行跟踪 | 从对话中提取计划执行反馈（"我今天跑了"→标记完成），更新执行进度 | 用户提及计划内容时 AI 能识别并更新状态 |
| 计划动态调整 | AI 发现用户连续未执行时主动询问是否需调整，或用户提出修改时实时更新 | 调整后计划同步更新，旧版本可回溯 |

**待完善方向：** 推理树拓展过程中需结合信息搜集机制（向用户提问——问答/选择题/确认等），让推理过程更具交互性，而非纯静态展开。后续将进一步设计交互式推理流程。

---

## 7. 健康计划系统设计

### 7.1 产品定位与理念

健康计划不是一份静态的"养生指南贴"，而是 Viora 根据对你的长期了解，量体裁衣制定的动态方案。

**核心原则：**
- **个性化，不通用** — 方案基于你的 routine、persona、profile notes 生成，不是百度百科式的通用建议
- **可执行，不压人** — 每天 5-8 个 actionable items，执行负担适中。理念 7（零压力）同样适用
- **活的，不是死的** — 计划与对话双向联动：AI 回复时参考计划，你的反馈又会更新计划
- **有科学依据，但不装专业** — 每条建议带简短的 reasoning，但语气仍是朋友式的

### 7.2 核心理念：推理树 = 计划本身

健康计划不是一次 LLM 调用直接生成的"建议列表"。计划生成的过程就是**用户参与、逐层推理**的过程——每个节点都是 LLM 在回答"这个病因/因素下，还有什么更具体的影响机制？"，一直拆到可以给出 actionable 建议为止（叶子节点）。

```
推理树的第一层                   脱发
                               ├── 雄激素性脱发
├── 休止期脱发     ← 病因层
                               │
用户点击展开↓                   │
                               │
推理树的第二层           雄激素性脱发
                               ├── 饮食因素
├── 运动因素
├── 压力因素      ← 因素层
                               │
用户继续点击↓                   │
                               │
推理树的第三层              高升糖饮食
                               └── 胰岛素↑ → DHT↑ → 毛囊攻击
├── 给出建议                         ← 机制层，此时可给出 action
```

**终止条件：** LLM 判断当前因素是否可以给出极度具体（做什么+做多少+什么时候做）的生活建议→能就是叶子，否则继续拆。

### 7.2 数据模型

#### 7.2.1 ReasoningNode（推理节点）

推理树的核心节点。**这是唯一的节点类型，叶子节点和中间节点共用同一结构。**

```python
@dataclass
class ReasoningNode:
    id: str                         # UUID
    label: str                      # "高升糖饮食" / "早餐全麦替代白面包"
    description: str                # 机制说明
    emoji: str                      # 🧬 🥗 🏃 😰 🍽️

    # 树结构
    depth: int                      # 层数（根节点=0）
    parent_id: str | None           # 父节点 ID
    children: list[ReasoningNode]   # 子节点列表
    expanded: bool = False          # 用户是否已展开（前端状态）
    expandable: bool = True         # 是否还可以继续展开（=还有下层）

    # 叶子节点专属（actionable 时填充）
    is_actionable: bool = False     # True=这是叶子，有具体建议
    action_text: str | None = None  # "早餐全麦面包替代白面包"
    time_slot: str | None = None    # "早餐" | "午餐" | "傍晚" | "睡前"
    frequency: str | None = None    # "每天" | "每周3次"
    duration: str | None = None     # "15分钟" | None
    reasoning: str | None = None    # 科学依据

    # 状态
    status: str = "pending"         # "pending" | "done" | "skipped" | "modified"
    completed_count: int = 0
    last_completed: str | None = None
    created_at: str = ""            # ISO timestamp
```

#### 7.2.2 HealthPlan（计划主体）

```python
@dataclass
class HealthPlan:
    id: str                               # UUID
    user_id: str
    concern: str                          # "最近脱发好严重"
    title: str                            # "🌿 头发养护计划"
    goal: str                             # 目标描述
    status: str                           # "active" | "archived" | "completed"
    created_at: str                       # ISO timestamp
    updated_at: str                       # 最后更新时间
    context_snapshot: dict                # 生成时的个性化上下文快照
    reasoning_tree: ReasoningNode | None  # 推理树根节点（核心数据）
    version: int = 1                      # 迭代版本号
```

`categories` 字段取消。计划列表视图改为**从推理树叶子节点实时聚合**——所有 `is_actionable=True` 的叶子节点按 `time_slot` 分组。

#### 7.2.3 PlanReminder（定时提醒）

保留不变：

```python
@dataclass
class PlanReminder:
    id: str
    plan_id: str
    user_id: str
    trigger_type: str = "time"
    time_spec: str = ""               # "15:00"
    days_of_week: list[int] | None    # [1,3,5] 或 None=daily
    message_template: str = ""
    is_active: bool = True
    last_triggered: str | None = None
    adaptive: bool = True
    created_at: str = ""
```

#### 7.2.4 存储架构

沿用现有 json + flock 模式，独立文件：

```python
HEALTH_PLANS_FILE = os.path.join(DATA_DIR, "health_plans.json")
PLAN_REMINDERS_FILE = os.path.join(DATA_DIR, "plan_reminders.json")
```

**health_plans.json 结构：**

```json
{
  "user_001": [
    {
      "id": "plan_xxx",
      "concern": "最近脱发好严重",
      "title": "🌿 头发养护计划",
      "goal": "改善脱发状况，促进头发健康生长",
      "status": "active",
      "created_at": "2026-06-01T10:30:00+08:00",
      "updated_at": "2026-06-01T10:30:00+08:00",
      "context_snapshot": { "...": "..." },
      "reasoning_tree": {
        "id": "root",
        "label": "脱发",
        "description": "用户主诉：最近脱发好严重",
        "emoji": "🧑‍🦲",
        "depth": 0,
        "parent_id": null,
        "expanded": true,
        "expandable": true,
        "is_actionable": false,
        "children": [
          {
            "id": "node_001",
            "label": "雄激素性脱发",
            "description": "遗传+激素，最常见类型...",
            "emoji": "🧬",
            "depth": 1,
            "expanded": false,
            "expandable": true,
            "is_actionable": false,
            "children": []
          }
        ]
      },
      "version": 1
    }
  ]
}
```

未展开的节点 `children` 为空数组，`expanded=false`。前端据此显示"···"可展开提示。

### 7.3 个性化数据源架构

健康计划的个性化数据源和推理树架构的关系：

```
┌──────────────────────────────────────────────────────────┐
│                  逐层推理树生成引擎                        │
│                                                          │
│  每一层展开时的 LLM Prompt =                              │
│    父节点信息 + Routine + Persona + Notes + 天气           │
└──────────────────────────────────────────────────────────┘
         ▲            ▲           ▲           ▲
         │            │           │           │
┌────────┴──┐ ┌──────┴──────┐ ┌──┴────────┐ ┌┴──────────┐
│ Routine   │ │ Persona     │ │ Profile   │ │ Weather   │
│ (routine  │ │ (persona.py)│ │ Notes     │ │ (weather  │
│  .py)     │ │            │ │(user_state│ │  .py)     │
│           │ │            │ │ .py)      │ │           │
│ 睡眠基线   │ │ 关心侧重    │ │ 生活背景   │ │ 城市     │
│ 运动频次   │ │ 表达风格    │ │ 个人偏好   │ │ 温度     │
│ 活跃时段   │ │ 建议强度    │ │ 长期困扰   │ │ 天气     │
│ 情绪基线   │ │            │ │ 当前目标   │ │           │
└───────────┘ └────────────┘ └──────────┘ └──────────┘
```

每一层展开调用 LLM 时，这些数据源都会注入 prompt，确保推理和建议始终个性化。

**不同 Persona 的推理树差异示例：**

| Persona | 推理树侧重 | 叶子节点语气 |
|---|---|---|
| 温暖好友 (默认) | 均衡覆盖各维度 | "晚餐加一道深绿色蔬菜，对头发好～" |
| 健身教练 | 运动分支优先展开 | "每天倒立30秒促进头皮供血，别偷懒" |
| 中医阿姨 | 食疗分支优先展开 | "少吃油炸，黑芝麻核桃粉每天一勺" |
| 理性分析师 | 深度拆解机制链 | "每天补充维B族，3周后评估掉发量变化" |

### 7.4 AI 生成引擎——逐层推理

**核心变化：不再有一次性的"生成整个计划"的 LLM 调用。每次调用只展开一个节点。**

#### 7.4.1 Expand API（单个节点展开）

`POST /api/plan/<id>/reasoning/expand` — 展开一个节点，生成其子节点。

**第一层展开（根节点 → 病因层）：**

当用户第一次点击根节点时，LLM 收到：

```
你是一个医学推理分析助手。请分析"{concern}"这个健康问题的可能病因分类。

【用户背景】
- 作息：{routine_summary}
- 个人情况：{profile_notes}
- 环境：{weather_context}

任务：列出 2-5 个最可能的病因方向。只到病因层，不要往下拆因素。

每个病因的要求：
- 一个简短名称
- 一句机制说明（1 句话）
- 一个代表 emoji

输出 JSON：
{
  "children": [
    {
      "label": "雄激素性脱发（遗传+激素）",
      "description": "最常见的脱发类型，与遗传和雄激素代谢相关",
      "emoji": "🧬"
    }
  ]
}
```

根节点展开时还同时生成：
- `title`（如 "🌿 头发养护计划"）
- `goal`（如 "改善脱发状况，促进头发健康生长"）

**中间层展开（因素/机制拆解）：**

当用户点击某个中间节点时，LLM 收到：

```
你正在分析：{parent_label}
它对"{concern}"的影响机制是：{parent_description}

【用户背景】
- 作息：{routine_summary}
- 个人情况：{profile_notes}

任务：
"{parent_label}"这个因素下，还有哪些更具体的子因素/机制需要分析？

对每个子因素，判断它是否可以直接给出极度具体的生活建议：
→ 能：标记 is_actionable=true，同时给出 action_text/time_slot/frequency/reasoning
→ 不能：标记 is_actionable=false，继续描述

极度具体 = 包含"做什么" + "做多少" + "什么时间"，如：
✅ "早餐全麦面包替代白面包" + "早餐" + "每天"
❌ "均衡饮食"（太笼统）

输出 JSON：
{
  "children": [
    {
      "label": "高升糖饮食",
      "description": "高升糖食物→胰岛素升高→促进雄激素活性→DHT增强→毛囊攻击",
      "emoji": "🍚",
      "is_actionable": false,
      "expandable": true
    },
    {
      "label": "早餐全麦面包替代白面包",
      "description": "全麦为低GI食物，不引起胰岛素急剧升高",
      "emoji": "🍽️",
      "is_actionable": true,
      "expandable": false,
      "action_text": "早餐全麦面包替代白面包",
      "time_slot": "早餐",
      "frequency": "每天",
      "duration": null,
      "reasoning": "全麦面包低GI→胰岛素稳定→DHT活性降低→减少毛囊攻击"
    }
  ]
}
```

**关键设计：LLM 必须自己判断"该停还是该拆"。** 这个判断标准写进 prompt：

> 如果你能给出一个同时包含"做什么 + 做多少 + 什么时候做"的极度具体建议，就标记 actionable。否则继续向下拆解。

#### 7.4.2 提取提醒 Prompt（不变）

保留原有方案的 `REMINDER_EXTRACT_PROMPT`，用于从对话中提取定时提醒。

#### 7.4.3 计划反馈处理 Prompt（不变）

保留原有方案的 `PLAN_FEEDBACK_PROMPT`，用于从对话中检测"完成了""调整计划"等意图。

### 7.5 对话联动机制

这是健康计划的核心——计划不是一个页面，而是融入对话体验的活系统。

#### 7.5.1 Chat → Plan：对话中的计划意图检测

在 `ResponsePlanner.decide_intent()` 中新增意图：

| 意图 | 触发关键词 | 响应行为 |
|---|---|---|
| `plan_request` | 制定计划、健康计划、帮我规划、调理方案、怎么办 | 创建推理树根节点，返回推理图谱页面入口 |
| `plan_reminder` | 提醒我、记得提醒、每天提醒、定时提醒 | 提取提醒参数 → 创建 reminder |
| `plan_feedback` | 跑了、吃了、做了、试了（配合计划上下文） | 解析反馈 → 更新叶子节点 status |
| `plan_modify` | 改一下计划、调整计划、不想、太麻烦 | 标记需修改 → AI 主动询问调整方案 |

#### 7.5.2 Plan → Chat：上下文注入

每次 chat 回复时，在 system prompt 中注入当前计划中**已采纳的叶子节点**。修改 `build_chat_context()` 或 chat 接口中的 prompt 构建逻辑：

```python
def build_plan_context(user_id: str) -> str:
    """从推理树中提取已采纳的叶子节点，注入 AI system prompt。"""
    plan = get_active_plan(user_id)
    if not plan or not plan.reasoning_tree:
        return ""

    # 收集所有标记为 done 或 pending 的叶子节点
    leaves = _collect_actionable_leaves(plan.reasoning_tree)
    if not leaves:
        return ""

    items_text = "
".join(
        f"  • {leaf.time_slot} → {leaf.action_text}（{leaf.reasoning}）"
        for leaf in leaves[:8]  # 最多 8 条
    )

    return (
        f"
【用户当前健康计划】
"
        f"计划目标：{plan.goal}
"
        f"今日安排：
{items_text}
"
        f"注意：自然地参考这些信息，不要生硬地念计划。"
    )
```

#### 7.5.3 Plan → Chat：定时提醒（不变）

保留原有方案的 `check_plan_reminders()` 逻辑，与推理树无关。

#### 7.5.4 动态计划调整

当 AI 通过 routine 异常检测或对话反馈发现用户连续未执行某项叶子 action 时：

```
检测逻辑：
  routine.check_plan_adherence()
  → 对比最近 N 天某叶子的 completed_count vs 其 frequency

主动消息生成：
AI: "看你最近几天都没去跑步，是计划强度太大了还是最近太忙了？
    要不要帮你调低一点频率？"

用户: "最近太忙了，改成一周两次吧"

→ 更新该叶子节点的 frequency
→ 该叶子父节点的推理路径保持不变
```

### 7.6 API 设计

核心变化：`POST /api/plan/generate` 不再一次性生成完整计划，而是创建推理树根节点；新增 Expand API。

| Method | Endpoint | 用途 | 请求体 / 参数 | 返回 |
|---|---|---|---|---|
| `POST` | `/api/plan/generate` | 创建新计划（推理树根节点） | `{"concern": "..."}` | HealthPlan（仅根节点） |
| `POST` | `/api/plan/<id>/reasoning/expand` | 展开指定节点 | `{"node_id": "..."}` | 展开后的子节点列表 |
| `GET` | `/api/plan` | 获取当前 active 计划 | — | HealthPlan（含推理树当前状态） |
| `GET` | `/api/plans` | 列出所有历史计划 | `?status=all` | `[HealthPlan, ...]` |
| `GET` | `/api/plan/<id>` | 获取指定计划详情 | — | HealthPlan |
| `PUT` | `/api/plan/<id>/status` | 切换状态 | `{"status": "archived"}` | 更新后的 Plan |
| `DELETE` | `/api/plan/<id>` | 删除计划 | — | `{"success": true}` |
| `PUT` | `/api/plan/<id>/items/<item_id>` | 更新叶子节点状态 | `{"status": "done"}` | 更新后的 Plan |
| `POST` | `/api/plan/<id>/regenerate` | 清空推理树重新生成 | — | 新版本 HealthPlan |
| `POST` | `/api/plan/reminder` | 创建提醒 | `{"action":"...","time_spec":"15:00"}` | PlanReminder |
| `GET` | `/api/plan/reminders` | 列出当前提醒 | — | `[PlanReminder, ...]` |
| `DELETE` | `/api/plan/reminder/<id>` | 删除提醒 | — | `{"success": true}` |

**Expand API 细节：**

```
POST /api/plan/<plan_id>/reasoning/expand
Body: {"node_id": "node_001"}

Response:
{
  "node_id": "node_001",
  "children": [
    {
      "id": "node_002",
      "label": "饮食因素",
      "emoji": "\U0001f957",
      "depth": 2,
      "expandable": true,
      "is_actionable": false
    },
    {
      "id": "node_003",
      "label": "早餐全麦面包替代白面包",
      "emoji": "\U0001f37d\ufe0f",
      "depth": 2,
      "expandable": false,
      "is_actionable": true,
      "action_text": "早餐全麦面包替代白面包",
      "time_slot": "早餐",
      "frequency": "每天"
    }
  ]
}
```

**chat 接口增强：** `POST /api/chat` 在返回中新增字段：

```json
{
  "extracted": {...},
  "response": "...",
  "plan_action": {
    "type": "plan_created",
    "plan_id": "plan_xxx",
    "message": "已为你创建脱发分析计划，点击探索推理树吧"
  }
}
```

### 7.7 前端页面设计

#### 7.7.1 导航 Tab

现有图谱 tab 增加一个子模式切换：

```html
<div class="graph-mode-tabs">
  <button class="mode-tab" data-mode="event">事件层</button>
  <button class="mode-tab" data-mode="state">状态层</button>
  <button class="mode-tab" data-mode="trajectory">轨迹层</button>
  <button class="mode-tab" data-mode="personality">Personality</button>
  <button class="mode-tab active" data-mode="reasoning">推理</button>
</div>
```

当有 active plan 且有推理树时，推理 tab 显示小红点。
切换到"推理"模式时，D3 渲染推理树而非普通图谱。

#### 7.7.2 推理图谱交互设计

**初始状态（只有根节点）：**

```
┌──────────────────────────────────────────────────┐
│ 🌿 健康分析 · 脱发                               │
│                                                  │
│  ┌───────── 推理图谱 ─────────┐                   │
│  │                            │                   │
│  │       🧑\u200d🦲 脱发      │                   │
│  │        点击探索 ▶           │                   │
│  │                            │                   │
│  └────────────────────────────┘                   │
│                                                  │
│  💡 点击每个节点，看看你的身体                      │
│     问题背后有哪些因素在影响                      │
│     一直点到底，就能找到为你                       │
│     量身定做的健康建议 ✨                         │
└──────────────────────────────────────────────────┘
```

**展开一层后：**

```
       🧑\u200d🦲 脱发
       ├── 🧬 雄激素性脱发 ···
       ├── ⚡ 休止期脱发 ···
       └── 🌀 斑秃 ···
```

**全部展开后（树的某个分支到达叶子）：**

```
       🧑\u200d🦲 脱发
       ├── 🧬 雄激素性脱发
       │   ├── 🥗 饮食因素
       │   │   ├── 🍚 高升糖饮食
       │   │   │   └── 🍽️ 早餐全麦面包替代白面包  ← 叶子
       │   │   └── 🥦 抗氧化不足
       │   │       └── 🍽️ 每天一把核桃/杏仁       ← 叶子
       │   └── 🏃 运动因素 ···
       ├── ⚡ 休止期脱发 ···
       └── 🌀 斑秃 ···
```

#### 7.7.3 节点交互规则

| 节点状态 | 视觉 | 点击行为 |
|---|---|---|
| **未展开（可展开）** | 底部有半透明 "···" 提示 | 触发 Expand API → 节点展开 |
| **展开中** | 脉冲动画 / shimmer 占位框 | 不可点击 |
| **已展开** | 子节点在下方展开，自身变灰/锚点样式 | 折叠/高亮该分支 |
| **叶子节点（未采纳）** | 淡绿背景 + 行动类 emoji + 悬停 tooltip | 右侧弹出详情面板 |
| **叶子节点（已采纳）** | 绿色背景 + ✅ 标记 | 右侧弹出详情面板 |

#### 7.7.4 右侧详情面板

点击叶子节点时，滑入详情面板：

```
┌─────────────────────┐
│ 🍽️ 饮食建议           │
│                      │
│ 早餐全麦面包替代白面包 │
│                      │
│ ⏰ 早晨 · 每天        │
│                      │
│ 📖 科学依据            │
│ 全麦面包为低GI食物，    │
│ 不会引起胰岛素急剧升高  │
│ → DHT活性降低          │
│                      │
│ 推理路径：             │
│ 脱发 → 雄激素性 →      │
│ 饮食 → 高升糖 → 此建议  │
│                      │
│ ┌──────────────────┐ │
│ │  ✓ 采纳到我的计划  │ │
│ └──────────────────┘ │
└─────────────────────┘
```

"推理路径"高亮从根节点到当前叶子的整条路径。

#### 7.7.5 计划列表视图（叶子聚合）

图谱旁边显示"已采纳建议"面板，所有叶子节点的聚合：

```
┌─ ✅ 已采纳建议（3/6）──────────────────┐
│                                        │
│ ✅ 早餐全麦面包替代白面包   早晨 · 每天 │
│ ✅ 每天一把核桃/杏仁        下午 · 每天 │
│ ✅ 每周4次30分钟快走        傍晚 · 每周4 │
│ ⬜ 力量训练≤3次/周          下午 · 每周3 │
│                                        │
│ 剩余 3 个叶子待探索 →                   │
└────────────────────────────────────────┘
```

**两个视图同根同源：** 推理图谱是树的完整展示，计划列表是所有叶子节点的聚合。
修改叶子的 status 在两个视图中同步更新。

### 7.8 与现有系统集成

| 现有系统 | 集成方式 | 改动范围 |
|---|---|---|
| `health_plan.py` → `ReasoningNode` 模型 + CRUD | 推理树存储与展开状态管理 | 新增 `ReasoningNode` dataclass、expand_node()、get_node()、collect_leaves() |
| `ai_engine.py` → `_call_llm` | 逐层扩展的 LLM 调用 | 新增 `EXPAND_ROOT_PROMPT`、`EXPAND_CHILD_PROMPT` 常量；新增 `expand_node()` 函数 |
| `app.py` → Flask routes | 新增 Expand API + 修改 generate API | 新增 `POST /api/plan/<id>/reasoning/expand`、修改 `POST /api/plan/generate` |
| `index.html` → D3 推理树渲染 | 图谱视图新增"推理"模式 | 新增树形布局、节点点击交互、详情面板、采纳逻辑 |
| `routine.py` → `RoutineModel` | 计划生成时获取作息基线 | 无改动，直接调用 |
| `persona.py` → `get_active_persona` | 影响推理树侧重方向 | 无改动，直接调用 |
| `storage.py` → flock + JSON | 沿用相同文件锁和数据持久化模式 | `reasoning_tree` 嵌套在 `health_plans.json` 内 |
| `scheduler.py` → SchedulerState | 新增 plan reminder 检查 | 无改动，沿用相同逻辑 |

### 7.9 文件变更清单

| 操作 | 文件 | 说明 |
|---|---|---|
| **修改** | `health_plan.py` | 删除 `PlanCategory`/`PlanItem` 数据模型；新增 `ReasoningNode` 模型；修改 `HealthPlan` 增加 `reasoning_tree` 字段；新增 `expand_node()`、`collect_actionable_leaves()`、`adopt_leaf()`；修改 `get_today_items()` 从树中聚合 |
| **追加** | `ai_engine.py` | 删除 `HEALTH_PLAN_PROMPT`；新增 `EXPAND_ROOT_PROMPT`（首次展开）、`EXPAND_CHILD_PROMPT`（递归展开）；新增 `expand_node()` 函数（单次 LLM 调用） |
| **修改** | `app.py` | 修改 `POST /api/plan/generate` 为创建推理树根节点；新增 `POST /api/plan/<id>/reasoning/expand`；新增 `GET /api/plan/<id>/reasoning/tree`；修改 chat 接口 plan_action 返回 |
| **修改** | `index.html` | 图谱视图新增"推理"模式 tab；D3 树形布局渲染树状推理图；节点点击交互（展开/折叠/采纳）；详情滑动面板；已采纳建议聚合列表 |

### 7.10 实施优先级

**Phase 4-A（本阶段核心 — 推理树数据模型 + Expand API）**
1. `ReasoningNode` 数据模型 + 序列化（`health_plan.py`）
2. Expand prompt 编写 + `expand_node()` 函数（`ai_engine.py`）
3. `POST /api/plan/generate` 改造 + `POST /api/plan/<id>/reasoning/expand` API（`app.py`）
4. 前端"推理"模式 tab + D3 树形布局渲染（`index.html`）
5. 节点点击 + 展开/折叠交互（`index.html`）

**Phase 4-B（增强体验）**
6. 叶子节点详情面板 + 采纳逻辑
7. 已采纳建议聚合列表视图
8. chat 中 plan_action 通知 + 推理路径展示

**Phase 4-C（完善）**
9. 计划提醒 Agent（scheduler 集成，沿用原有逻辑）
10. 计划执行跟踪 + 动态调整（对话中标记完成）

---

### Phase 5：Android 移动端 APK 工程

**目标：基于 Chaquopy + WebView 构建 Viora Android APK，作为 Phase 6 BLE 穿戴通信的宿主容器，同时支持消息推送、后台服务等移动核心能力**

**状态：📋 规划中**

#### 5.1 方案对比：WebAPK vs 原生 APK

| 维度 | WebAPK (PWA) | 原生 APK (Kotlin + WebView) | 混合 (Chaquopy + WebView) |
|---|---|---|---|
| **开发成本** | 极低（现有 Web 直接可用） | 高（需重写 UI） | 中（Web 逻辑复用） |
| **穿戴数据访问** | ❌ 无法访问本地 DB | ✅ 完整 Android API | ✅ 完整 Android API |
| **后台服务** | ❌ 有限 | ✅ Service/WorkManager | ✅ 通过 Python |
| **消息推送** | ⚠️ 依赖浏览器推送 | ✅ FCM 完整支持 | ✅ FCM 完整支持 |
| **BLE 能力** | ❌ Web Bluetooth 有限 | ✅ 完整 BLE API | ✅ 通过原生桥接 |
| **文件系统** | ❌ 沙箱限制 | ✅ 完整访问 | ✅ 完整访问 |
| **更新方式** | 自动（浏览器） | Play Store / 侧载 | Play Store / 侧载 |
| **APK 大小** | 极小（~1MB） | ~ 5-10MB | ~ 15-25MB（含 Python） |
| **维护成本** | 无 | 中 | 中 |

**推荐方案：Chaquopy + WebView 混合 APK**

[Chaquopy](https://chaquo.com/chaquopy/) 是 Android Studio 的 Gradle 插件，可以在 Android App 中嵌入 Python 运行时，直接运行现有 Flask 后端代码。

**架构（含 Phase 6 BLE 模块）：**

```
┌────────────────────────────────────────────────────────────┐
│  Viora Android App                                             │
│                                                                │
│  ┌──────────────────────┐  ┌──────────────────────────────┐  │
│  │  Kotlin 原生层        │  │  Chaquopy (Python 运行时)     │  │
│  │                      │  │                              │  │
│  │  • BLE Service      │──│→│  Viora Python 后端          │  │
│  │    (Phase 6 穿戴通信) │  │  │  (app.py + feedback.py    │  │
│  │  • FCM 推送          │  │  │   + storage.py + ...)     │  │
│  │  • 后台 Service      │  │  │   + wearable_parser.py    │  │
│  │  • 本地 HTTP Server  │  │  │                            │  │
│  │    (NanoHTTPD:9500)  │  │  │  本地 HTTP (127.0.0.1:5050)│  │
│  └──────────┬───────────┘  │  └──────────────────────────────┘  │
│             │              │  ┌──────────────────────────────┐  │
│             └──────────────│→│  前端 WebView                 │  │
│                            │  │  (index.html + D3.js)        │  │
│                            │  │  http://127.0.0.1:5050       │  │
│                            │  └──────────────────────────────┘  │
│                            │                                    │
│  ┌─────────────────────────┴──────────────────────────────┐    │
│  │  Wearable BLE ⇄ 小米手环 / Amazfit / 其他设备           │    │
│  └────────────────────────────────────────────────────────┘    │
└────────────────────────────────────────────────────────────────┘
```

**优势：**
- Python 代码（app.py、storage.py、feedback.py 等）**零修改直接运行**
- 前端页面（index.html）通过 WebView 渲染，**无需重写 UI**
- Kotlin 原生层处理 BLE 通信（Phase 6）和推送等系统能力
- 本地 HTTP Server 持久运行，WebView 通过 `127.0.0.1:5050` 访问
- 支持离线运行（所有存储基于本地 JSON）

#### 5.2 实施计划

**Phase 5-A（最小可行 APK — WebView + 本地后端 + BLE 基础）**

| 功能 | 描述 | 预估工时 |
|---|---|---|
| 1. Android Studio 项目初始化 + Chaquopy 配置 | Gradle 配置，Python 环境嵌入 | 1 天 |
| 2. 将 Viora Python 代码移植到 Assets | app.py + 依赖包打包进 APK | 1 天 |
| 3. WebView 组件 + 本地 HTTP 通信 | WebView 加载本地 Server，前后端打通 | 1 天 |
| 4. Phase 6 BLE 模块集成 | 将提取的 BtLEQueue + Huami 协议纳入 APK | 见 Phase 6 |
| 5. APK 构建与基础测试 | 确保所有 API 在 Android 上正常工作 | 1 天 |

**Phase 5-B（原生能力增强）**

| 功能 | 描述 |
|---|---|
| 6. 消息推送（FCM） | 主动消息在手机通知栏显示 |
| 7. 后台 Service | App 在后台时 Python 后端 + BLE Service 持续运行 |
| 8. BLE 重连与状态管理 | 手环断连自动重连，连接状态在前端显示 |
| 9. 离线模式支持 | 无网络时本地运行，联网后同步 |

**Phase 5-C（体验完善）**

| 功能 | 描述 |
|---|---|
| 10. Android Widget | 桌面小部件显示今日健康摘要 |
| 11. 系统通知适配 | 主动关心通过系统通知推送，点击进入对话 |
| 12. Play Store 上架准备 | 隐私政策、权限声明、应用截图 |
| 13. 国内应用商店上架 | 华为、小米、OPPO、vivo 等商店适配 |

#### 5.3 快速开始（Phase 5-A 技术栈）

```bash
# Android 端
Android Studio Hedgehog+
Chaquopy 15.0.1+
Python 3.11+
minSdk 26 (Android 8.0)
targetSdk 34 (Android 14)

# Python 端（嵌入 APK）
Flask 3.x
numpy（用于关联计算）
requests（用于天气 API）
```

---

### Phase 6：穿戴设备直连（内嵌 Gadgetbridge BLE 通信层）

**目标：将 Gadgetbridge 的 BLE 穿戴设备通信能力直接嵌入 Viora APK，用户安装一个 App 即可直连手环/手表，实现客观健康数据自动采集并融入图谱**

**状态：📋 规划中**

#### 6.1 方案概述

[Gadgetbridge](https://codeberg.org/Freeyourgadget/Gadgetbridge) 是 GPLv3 开源项目，支持 200+ 款穿戴设备。其核心 BLE 通信层（`service/btle/`，~11K LOC / ~40 文件）和设备协议实现（`devices/` + `service/devices/`）架构清晰，可以从其单体项目中提取出来嵌入 Viora APK。

**关键优势：**
- 用户只需安装 Viora 一个 App，不需要额外装 Gadgetbridge
- 所有 BLE 通信在 Viora APK 的 Kotlin 原生层运行
- Python 后端通过本地 HTTP API 获取穿戴数据，零耦合
- GPLv3 发布，合规使用开源代码

**可获取的数据类型（首批支持设备 — 小米手环 8/9）：**

| 数据类型 | 说明 | Viora 维度映射 |
|---|---|---|
| 步数 / 日常活动量 | 日/小时级步数统计 | 新增 `activity` 维度 |
| 心率（静息/连续） | 含时间戳序列 | 新增 `heart_rate` 维度 |
| 睡眠（时长/阶段） | 深度/浅度/REM/清醒 | 增强 `sleep`（客观对照） |
| 血氧 SpO2 | 部分设备支持 | 新增 `spo2` 维度 |
| 压力指数 | 基于 HRV 计算 | 新增 `stress` 维度 |
| 消耗卡路里 | 基础代谢 + 活动消耗 | 新增 `calories` 维度 |
| 运动记录 | 自动识别/手动开始 | 增强 `exercise` 维度 |
| 心率变异性 HRV | 睡眠期间测量 | 新增 `hrv` 维度 |

> 以上为小米手环 8/9 支持的数据。后续设备按相同协议框架扩展。

#### 6.2 嵌入方式

**从 Gadgetbridge 源码中提取以下模块嵌入 Viora APK：**

| 模块 | 来源（Gadgetbridge 包路径） | 说明 |
|---|---|---|
| BLE 传输层 | `service/btle/`（~40 文件 / 11K LOC） | BtLEQueue、TransactionBuilder、GATT 回调等 |
| 设备协调器 | `devices/huami/`（~25 文件） | 设备发现、能力声明、配对流程 |
| 设备协议 | `service/devices/huami/`（~30 文件） | Mi Band 数据读取、命令发送 |
| 数据模型 | `model/` + `impl/`（~50 文件） | ActivitySample、HeartRateSample 等 |
| Protobuf 定义 | `proto/huami.proto` | Huami 私有协议序列化 |

**不需要的部分（占 Gadgetbridge 70%+ 的 UI 代码）：**
- 所有 Activity/Fragment/布局文件
- 通知转发（NotificationListenerService）
- 来电/SMS 转发
- 固件升级（DFU）
- 音乐控制、天气推送
- 日历/闹钟管理
- 设备设置页面
- GreenDAO 数据库层（替换为 Viora JSON 存储）

**架构图：**

```
┌───────────────────────────────────────────────────────────────────────┐
│ Viora Android App（单 APK）                                              │
│                                                                        │
│  ┌────────────────────────────┐  ┌──────────────────────────────────┐  │
│  │  Kotlin 原生层              │  │  Chaquopy (Python 运行时)        │  │
│  │                            │  │                                  │  │
│  │  ┌──────────────────────┐  │  │  ┌──────────────────────────┐   │  │
│  │  │ Wearable BLE Service │  │  │  │  Viora Python 后端       │   │  │
│  │  │  (Android Service)   │  │  │  │  (app.py, storage.py,    │   │  │
│  │  │                      │  │  │  │   health_graph.py, ...)  │   │  │
│  │  │  ┌───────────────┐   │  │  │  │                          │   │  │
│  │  │  │ BtLEQueue     │   │  │  │  │  • 解析穿戴数据 schema   │   │  │
│  │  │  │ (GATT 事务队列)│   │  │  │  │  • 合并到 health_graph  │   │  │
│  │  │  └───────────────┘   │  │  │  │  • AI 融合推理           │   │  │
│  │  │  ┌───────────────┐   │  │  │  └──────────────────────────┘   │  │
│  │  │  │ HuamiProtocol │   │  │  │                                  │  │
│  │  │  │ (Mi Band 协议) │   │  │  │  ┌──────────────────────────┐   │  │
│  │  │  └───────────────┘   │  │  │  │  WebView                │   │  │
│  │  │  ┌───────────────┐   │  │  │  │  (index.html + D3.js)    │   │  │
│  │  │  │ Data Store    │   │──│──│→│                          │   │  │
│  │  │  │ (本地 JSON)    │   │  │  │  │  http://127.0.0.1:5050  │   │  │
│  │  │  └───────────────┘   │  │  │  └──────────────────────────┘   │  │
│  │  └──────────────────────┘  │  └──────────────────────────────────┘  │
│  │                            │                                        │
│  │  ┌──────────────────────┐  │                                        │
│  │  │ 本地 HTTP Server     │  │                                        │
│  │  │  (NanoHTTPD,         │──│─────────────────────────────────────────│
│  │  │   127.0.0.1:9500)   │  │                                         │
│  │  └──────────────────────┘  │                                        │
│  └────────────────────────────┘                                        │
│                                                                        │
│         ┌─────────────────────────────────────┐                        │
│         │  BLE ⇄ 小米手环 8/9 / Amazfit / …    │                        │
│         └─────────────────────────────────────┘                        │
└───────────────────────────────────────────────────────────────────────┘
```

**关键设计决策：**
- Kotlin 原生层运行 BLE 通信，不经过 Python （Android BLE API 只能从原生层调用）
- BLE Service 独立运行，采集的数据写入本地 JSON 文件
- Python 后端通过本地 HTTP API (`127.0.0.1:9500`) 读取穿戴数据
- 数据存储复用 Viora 现有 JSON + flock 模式

#### 6.3 首批设备支持 — 小米手环 8/9

**选择理由：**
1. 国内市占率最高，用户基数最大
2. Huami 协议在 Gadgetbridge 中实现最成熟、持续维护
3. 价格低廉（¥200-300），开发测试成本低
4. 支持完整的数据类型（心率、睡眠、血氧、压力、运动）

**需从 Gadgetbridge 提取的核心文件（Mi Band 8/9）：**

```
service/btle/                      → BLE GATT 通信层
service/btle/actions/              → BLE 操作原语
service/btle/profiles/             → 标准 GATT Profile
devices/huami/HuamiCoordinator.java → 设备协调器
devices/huami/miband8/             → Mi Band 8/9 具体实现
service/devices/huami/HuamiSupport.java → 协议主逻辑
service/devices/huami/operations/  → 具体操作（获取心率、同步睡眠等）
model/                             → 数据模型
proto/huami.proto                  → 序列化协议
```

#### 6.4 数据融合策略

穿戴数据与聊天数据的融合逻辑与之前一致：

```
客观数据（穿戴 BLE）         主观数据（聊天）
     │                           │
     ▼                           ▼
  心率 78 bpm                  "今天心跳好快"
  睡眠 6.2h（深度1.5h）         "没睡好，醒了两次"
  步数 8234                    "今天走了不少路"
     │                           │
     └────────┬──────────────────┘
              ▼
     ┌───────────────────┐
     │  AI 融合推理        │
     │  "你昨晚深睡只有     │
     │   1.5小时，但你说    │
     │   睡得还行，看起来    │
     │   你对自己睡眠感知    │
     │   偏乐观"          │
     └───────────────────┘
```

**核心交互场景：** 数据印证、数据矛盾、数据补全、趋势预警（同前）

#### 6.5 实施计划

**Phase 6-A（MVP — Mi Band BLE 通信 + 数据入库）**

| # | 功能 | 描述 | 预估工时 |
|---|---|---|---|
| 1 | Fork Gadgetbridge，分析 BLE 通信层 | 本地 clone，划定需提取的代码范围 | 1 天 |
| 2 | 从 Gadgetbridge 提取 BtLEQueue + GATT 层 | `service/btle/` 及其依赖，适配为独立模块 | 2-3 天 |
| 3 | 提取 Huami/Mi Band 8 协议实现 | `devices/huami/` + `service/devices/huami/` | 2-3 天 |
| 4 | 在 Viora APK 中搭建 Wearable BLE Service | Android Service，管理 BLE 连接生命周期 | 2 天 |
| 5 | 实现本地数据存储（JSON） | BLE 采集数据写入 Viora JSON 文件 | 1 天 |
| 6 | 定义穿戴数据 schema + Python 解析器 | 后端新维度结构和数据清洗 | 1 天 |
| 7 | 扩展 health_graph 维度列表 | 注册新维度到关联计算引擎 | 0.5 天 |
| 8 | AI prompt 增强 | 让 AI 理解并引用客观数据 | 1 天 |
| 9 | 端到端联调 | 手环 BLE → Viora → 图谱 → AI 回复 | 2 天 |
| | **合计** | | **~13-16 天** |

**Phase 6-B（更多设备 + 数据融合）**

| # | 功能 | 描述 |
|---|---|---|
| 10 | 更多设备支持（Amazfit / PineTime / …） | 按相同框架提取对应协议 |
| 11 | 主客观数据对比引擎 | 同一维度下对比聊天与穿戴一致性 |
| 12 | AI 感知偏差对话模式 | AI 基于数据差异自然对话（非评判式） |
| 13 | 趋势预警 | 设定基线偏离阈值，触达时主动关心 |
| 14 | 穿戴数据时间线可视化 | 在图谱/故事中叠加客观数据曲线 |

**许可说明：**
- Gadgetbridge 采用 **GPLv3** 协议
- Viora 嵌入其代码后整体也需以 GPLv3 发布
- 不影响 Python Flask 后端和前端 HTML/JS 的许可证选择
- 私有部署/商业分发需注意 GPLv3 合规要求

---

## 8. Agent 编排体系 — 医学严肃性与严谨性保障

> ⚠️ **状态：需要我来开发，不可自动推进。Agent 不得自行启动此工作。**

### 8.1 背景

Viora 的核心交互同时涉及两个目标：**交互体验的自然感**（像朋友聊天）和**医学内容的严谨性**（不乱说不瞎说）。随着系统复杂度提升（聊天、故事、图谱洞察、健康计划），单一 prompt 已经难以兼顾两者。

需要通过多 Agent 编排，将不同职责分离到专门的 Agent 中，各自强化对应领域的专业边界和校验逻辑，最终由编排层确保：**友好但不轻浮，严谨但不教条。**

### 8.2 四大 Agent 职责划分

| Agent | 核心职责 | 核心约束 |
|---|---|---|
| **用户交互端** (Interaction Agent) | 管理对话节奏、语气、Persona；决定何时开口、说什么话；处理 Message Burst 和 emoji 策略 | **禁止自行给出医学结论或建议措辞**；所有与健康相关的内容必须引用分析端的输出 |
| **故事生成端** (Story Agent) | 将结构化健康数据编织为可读的身体故事叙事（时间线叙事、周报总结） | **所有陈述必须可追溯到原始数据；不确定的关联必须标注置信度和样本量** |
| **分析端** (Analysis Agent) | 从对话/数据中发现趋势、关联、异常；生成图谱洞察和 Routine 更新 | **所有分析结论必须引用数据来源、时间窗口、样本量；禁止过度推断；禁止单样本断言** |
| **计划生成端** (Planning Agent) | 基于用户主诉和画像生成推理树、给出 actionable 的生活建议 | **每条建议必须有科学依据链（reasoning）；禁止给出超出"生活方式干预"范围的医学建议** |

### 8.3 编排原则

1. **职责分离、互不越界** — Interaction Agent 不分析，Analysis Agent 不和用户直接对话，Planning Agent 不绕过交互端推送内容
2. **分析先行、交互收口** — 所有面向用户的内容，需确认不与分析结论冲突；故事和计划的内容来源必须标记是哪个分析节点产出的
3. **可追溯链** — 任何一条输出（故事片段、分析洞察、计划建议）都能回溯到具体的输入数据、推理路径、置信度
4. **安全边界硬编码** — 在编排层（而非 prompt 层）禁止特定行为：不做诊断、不开处方、不替代医生、不制造恐慌

### 8.4 待我确认的开放问题

以下问题需我亲自确认后才能进入开发，不可由 Agent 自行推进：

- [ ] 各 Agent 间的通信协议和调度方式（同步 pipeline vs 异步消息队列 vs 事件驱动）
- [ ] 安全校验层的实现方式（rule-based gate vs LLM-as-judge vs 混合方案）
- [ ] 故事生成端和分析端的协作模式（分析端先产出结论 → 故事端包装叙事；还是并行输出后由编排层合并？）
- [ ] 计划生成端与交互端的衔接策略（计划生成后如何通过交互端自然传递给用户？是"推送通知"模式还是"下次聊天时注入"？）
- [ ] 医学知识库的引入范围与更新策略（是否引入结构化医学知识库？引入范围？更新频率？）
- [ ] 四大 Agent 的实现方式（独立的 LLM 调用 vs 同一个 LLM 的不同 system prompt vs 混合）
- [ ] 编排层技术选型（plain Python 函数调度 vs 轻量状态机 vs Agent 框架如 LangGraph/CrewAI）

### 8.5 推进前置条件

- [ ] 我亲自确认上述开放问题的答案
- [ ] 我确认各 Agent 的详细职责边界文档
- [ ] 我确认安全校验硬规则清单
- [x] 完成当前 Phase 4-A（推理树数据模型 + Expand API）后再启动此 Agent 编排工作
- [ ] 此工作不阻塞当前 Phase 3/4 的日常推进

---

## 9. 用户心理画像体系 — 心理轨迹感知式 JITAI

> **核心命题：** 用户是否坚持运动，不只取决于一次运动后的成就感，而取决于其长期心理基线、当下心理状态、运动体验、环境阻力和 APP 干预之间的动态耦合。
>
> Viora 不只预测"用户会不会运动"，而是估计**用户现在处于什么心理状态、这种状态下哪种干预最可能降低阻力、提升下一次行动概率**。

### 9.1 设计动机

Viora 已有的用户建模体系（Routine、UserState、TrajectoryGraph）建模的是**可观测行为**——几点睡、运动了没、情绪几分。但同样的行为背后可能对应完全不同的心理动力学：

| 用户 | 表面行为 | 底层心理动力学 | 应该给的干预 |
|---|---|---|---|
| A | 经常不运动 | 自我效能低，但不排斥运动 | 给低门槛目标和成功经验 |
| B | 经常不运动 | 压力高，看到提醒更烦 | 减少催促，给恢复型建议 |
| C | 经常不运动 | 运动后痛苦感强 | 降低强度，强调舒适体验 |
| D | 经常不运动 | 没有目标意义感 | 绑定个人健康目标 |
| E | 经常不运动 | 断签后自责，容易放弃 | 设计"无惩罚恢复机制" |

**关键差距：** 现有系统能告诉你"用户没运动"，但不能告诉你"为什么没运动"和"现在该说什么"。心理画像体系填补了这个空白——它不是给 Viora 加一个"心理问卷模块"，而是**在现有对话流中自然生长出一套心理感知层**。

### 9.2 核心理念：两层心理建模

用户的运动心理不是一组静态分数，而是一个随时间变化的动态系统。拆成两层：

#### 第一层：心理基线（PsychoTrait）——慢变量

用户相对稳定的心理特征，不是每天大幅变化的：

| 心理基线 | 含义 | 对运动坚持的影响 |
|---|---|---|
| 自主性偏好 | 喜欢自己决定，还是接受系统安排 | 决定推送是否让人反感 |
| 奖励敏感性 | 是否容易被勋章、积分、等级激励 | 决定游戏化是否有效 |
| 压力敏感性 | 是否容易被连续打卡、排行榜压迫 | 决定打卡机制是否适得其反 |
| 初始自我效能 | 是否相信自己能坚持 | 决定目标要设多低 |
| 运动身份认同 | 是否认为"我是一个会运动的人" | 影响长期内化 |
| 社交暴露偏好 | 是否愿意分享运动成果 | 决定社区/海报是否有用 |
| 身体不适基线 | 疲劳、疼痛、睡眠差、慢病限制 | 决定运动阻力 |

#### 第二层：心理轨迹（PsychoEvent + PsychoState）——快变量

用户每天、每次运动前后变化的状态，适合 JITAI 的实时适配对象：

| 时刻 | 细粒度心理状态 |
|---|---|
| 运动前 | 意愿、疲劳、压力、拖延感、时间阻力、预期痛苦 |
| 运动中 | 愉悦、痛苦、无聊、控制感、身体负担 |
| 运动后 | 成就感、恢复感、挫败感、自责、下次信心 |
| 第二天 | 行动准备度、心理残留、是否产生回避 |
| 一周后 | 习惯感、断签挫败、目标认同变化 |

### 9.3 数据模型

#### 9.3.1 PsychoTrait（心理基线）

```python
@dataclass
class PsychoTrait:
    """长期稳定的心理画像参数，低频更新（月级）"""
    id: str
    user_id: str
    autonomy_preference: float       # 自主性偏好 0-1
    reward_sensitivity: float        # 奖励敏感性 0-1
    pressure_sensitivity: float      # 压力敏感性 0-1
    initial_self_efficacy: float     # 初始自我效能 0-1
    exercise_identity: float         # 运动身份认同 0-1
    social_exposure_pref: float      # 社交暴露偏好 0-1
    discomfort_baseline: float       # 身体不适基线 0-1
    confidence: float                # 整体置信度 0-1
    sample_count: int
    updated_at: str
```

**初始化策略：** 不做固定问卷。在用户对话意愿高的时候（比如某次聊得很投入），AI 用聊天式提问自然了解。例如：

> "说起来，你平时更喜欢自己安排时间还是跟着计划走？"
> "有些人看到打卡就很烦，有些人觉得有动力——你是哪种？"

结合 AI 自己的 personality（如毒舌闺蜜分享自己拖延经历，顺带问用户），让提问不像是"采集数据"，而像朋友之间的互相了解。初期置信度低时各参数默认为 0.5（中性），随着对话积累逐步校准。

#### 9.3.2 PsychoEvent（心理事件）

```python
@dataclass
class PsychoEvent:
    """单次心理事件，类似 EMA 的一次采样"""
    id: str
    user_id: str
    timestamp: str
    phase: str                      # "pre_exercise" | "during_exercise" |
                                     # "post_exercise" | "next_day" | "idle"
    # 心理状态快照（每项 0-10，仅在能观测到时填充）
    willingness: float | None        # 运动意愿
    fatigue: float | None            # 疲劳感
    stress: float | None             # 压力
    procrastination: float | None    # 启动阻力
    expected_pain: float | None      # 预期痛苦
    enjoyment: float | None          # 运动中愉悦
    boredom: float | None            # 无聊感
    control: float | None            # 控制感
    achievement: float | None        # 成就感
    recovery: float | None           # 恢复感
    frustration: float | None        # 挫败感
    guilt: float | None              # 自责
    next_confidence: float | None    # 下次信心
    avoidance: float | None          # 回避倾向
    source_type: str                # "chat_inferred" | "wearable"
    source_message_id: str
    confidence: float
```

#### 9.3.3 PsychoState（心理状态快照）

```python
@dataclass
class PsychoState:
    """聚合后的短期心理状态（3-7 天窗口）"""
    id: str
    user_id: str
    window_start: str
    window_end: str
    # 聚合值
    avg_willingness: float | None
    avg_fatigue: float | None
    avg_stress: float | None
    avg_self_efficacy: float | None
    avg_achievement: float | None
    avg_avoidance: float | None
    avg_guilt: float | None
    # 趋势
    willingness_trend: str          # "improving" | "declining" | "stable"
    self_efficacy_trend: str
    avoidance_trend: str
    # 可干预状态分类（核心输出）
    intervenable_state: str | None
    # "willing_but_stuck" | "low_mood_available" | "low_self_efficacy"
    # "post_exercise_frustrated" | "avoidance_after_break"
    # "high_achievement_fatigued" | "social_motivated" | "social_pressured"
    # "normal"
    event_ids: list[str]
    confidence: float
    updated_at: str
```

#### 9.3.4 PsychoFingerprint（心理轨迹指纹）

把用户心理建成类似"指纹"的动态响应系统：

```python
@dataclass
class PsychoFingerprint:
    """Psychological Trajectory Fingerprint — 用户的动态响应系统"""
    user_id: str
    baseline: PsychoTrait | None         # μ_i: 心理基线
    volatility: dict[str, float]         # Σ_i: 各维度波动幅度
    state_inertia: dict[str, float]      # A_i: 状态惯性（昨天→今天的一阶自回归系数）
    intervention_sensitivity: dict[str, float]  # B_i: 对各种干预的响应强度
    recovery_days: float                 # R_i: 断签后恢复的期望天数
    confidence: float
    sample_count: int
    updated_at: str
```

| 组成 | 含义 | 示例 |
|---|---|---|
| μ_i (baseline) | 用户心理基线 | 平均疲劳 0.4，自我效能 0.65，压力 0.3 |
| Σ_i (volatility) | 心理波动幅度 | 情绪波动大（0.8），自我效能稳定（0.2） |
| A_i (state_inertia) | 状态惯性 | 今天没运动 → 明天不想动的概率 0.7 |
| B_i (intervention_sensitivity) | 干预敏感性 | 降门槛干预效果 0.8，社交激励效果 0.2 |
| R_i (recovery_days) | 复原能力 | 断签后平均 3.5 天恢复运动 |

### 9.4 数据采集机制

#### 9.4.1 EMA 轻量化采集：聊天式嵌入，不直接问

核心原则：**不直接问"你现在感觉怎么样？"，而是在自然对话中嵌入情境引导。**

| 时机 | 采集方式 | 示例 |
|---|---|---|
| 晚饭后 | 情境引导式 | "吃完饭了？后面还有什么安排？" → 用户说"赶论文" → 推断：压力↑、情绪可能偏低 |
| 运动时段 | 轻量观察 | "今天天气不错～"（不提运动）→ 用户主动提到/回避运动 → 推断意愿 |
| 运动后 | 自然追问 | "跑完啦？" → 用户说"比想象中轻松" → 推断：achievement↑、next_confidence↑ |
| 第二天 | 回顾式 | "昨天运动完今天感觉还行吗？" → 推断后续影响 |
| 周末/空闲 | 习惯话题 | AI 分享自己的运动经历，顺带了解用户偏好 |

**关键洞察：** EMA 式的提问是一种打扰，但打扰程度取决于提问方式。最好的心理状态采集不是"你今天压力大吗？"（侵入性强），而是"饭后有什么打算？"（朋友聊天）——心理信号从对话中自然渗出，用户甚至不觉得自己在"被采集"。

#### 9.4.2 被动推断

不需要主动问的信号，从行为和交互模式中推断：

| 信号来源 | 推断的心理状态 | 来源模块 |
|---|---|---|
| 用户连续 3 天没回消息 | avoidance↑, willingness↓ | `scheduler.py` |
| 用户频繁提到"累""困" | fatigue↑ | chat 健康提取 |
| 用户深夜发消息 | stress↑, 睡眠差 | `routine.py` |
| 计划提醒被忽略 | 干预敏感性变化 | `health_plan.py` |
| 用户说要运动但没做 | procrastination↑, guilt↑ | EventNode 对比 |
| 睡眠质量连续下降 | fatigue↑, stress↑, self_efficacy↓ | `routine.py` 异常检测 |
| 用户主动分享运动成果 | exercise_identity↑, achievement↑ | chat 意图检测 |
| 用户切换 Persona（如选毒舌闺蜜） | 间接反映社交偏好和压力耐受 | `persona.py` |

#### 9.4.3 对话→心理状态 提取 Prompt

在现有 `ai_engine.py` 健康维度提取之上，新增心理维度：

```
从用户消息中提取心理状态指标（0-10 分，仅提取明确表达的，不确定填 null）：

{
  "willingness": <运动意愿>,
  "fatigue": <疲劳感>,
  "stress": <压力感>,
  "procrastination": <启动阻力>,
  "achievement": <成就感>,
  "frustration": <挫败感>,
  "guilt": <自责感>,
  "next_confidence": <下次信心>,
  "avoidance": <回避倾向>
}

提取规则：
- 只在用户明确表达时提取，不推测
- "今天一点都不想动" → willingness=2, procrastination=8
- "跑完感觉还不错" → achievement=7, next_confidence=7
- "太累了"（运动上下文）→ fatigue=8
- "又没去，我真没用" → guilt=9, avoidance=7
```

### 9.5 心理状态推理引擎

#### 9.5.1 状态更新算法

指数衰减 EMA 更新当前心理状态：

```
z_{t+1} = α * z_t + (1-α) * event_value
```

不同维度使用不同的衰减率 α：

| 维度 | α | 理由 |
|---|---|---|
| fatigue | 0.6 | 疲劳变化快，受当天活动影响大 |
| stress | 0.6 | 压力随环境快速变化 |
| willingness | 0.5 | 意愿波动大，受即时情境影响 |
| self_efficacy | 0.85 | 自我效能变化慢，需多次成功体验才上升 |
| avoidance | 0.75 | 回避倾向中等惯性 |
| guilt | 0.5 | 自责来去都快 |
| achievement | 0.7 | 成就感延续一段时间后消退 |

#### 9.5.2 可干预状态分类

引擎的核心输出不是"用户的情绪是什么"，而是"用户现在处于什么可干预状态"：

| 可干预状态 | 识别规则 | 适合的干预 |
|---|---|---|
| `willing_but_stuck` | willingness > 0.6 AND procrastination > 0.5 | 降低启动门槛，给微运动入口 |
| `low_mood_available` | willingness 正常 AND mood < 0.4 | 低压力、舒缓型运动 |
| `low_self_efficacy` | self_efficacy < 0.3 | 极低目标 + 完成反馈 |
| `post_exercise_frustrated` | frustration > 0.6 | 降低下次目标，避免羞辱反馈 |
| `avoidance_after_break` | avoidance > 0.7 AND guilt > 0.6 | "恢复计划"，不强调断签 |
| `high_achievement_fatigued` | achievement > 0.7 AND fatigue > 0.6 | 鼓励恢复，避免过度训练 |
| `social_motivated` | social_exposure_pref > 0.6 | 给分享、组队、挑战 |
| `social_pressured` | social_exposure_pref < 0.3 | 默认私密，不推排行榜 |
| `normal` | 无显著偏离 | 常规聊天节奏，不干预 |

### 9.6 干预决策引擎

#### 9.6.1 从心理状态到干预策略

```python
class PsychoEngine:
    """心理状态推理引擎 → 干预策略推荐"""

    def recommend_intervention(self, user_id: str) -> dict:
        """
        基于 (当前心理状态 × 心理指纹 × 上下文) 推荐干预策略

        返回:
        {
            "intervention_type": "lower_barrier",  # 干预类型
            "intensity": "light",                   # 强度
            "timing": "now",                        # 时机
            "suggested_tone": "casual",             # 语气建议
            "confidence": 0.72,
            "reasoning": "用户self_efficacy低但willingness中等，降低门槛比鼓励更有效"
        }
        """
```

#### 9.6.2 干预匹配矩阵

| 可干预状态 | 干预类型 | Viora 行为策略 | 消息示例（温暖好友） |
|---|---|---|---|
| `willing_but_stuck` | `lower_barrier` | 给 3-5 分钟微运动入口 | "不用跑步，就下楼走5分钟～" |
| `low_mood_available` | `gentle_suggest` | 推荐散步/拉伸，不提强度 | "今天天气不错，出去透透气就好" |
| `low_self_efficacy` | `micro_goal` | 给"完成就行"的极简任务 | "今天就做一件事：穿上运动鞋。做到了就算赢😏" |
| `post_exercise_frustrated` | `normalize` | 避免说"加油"，说"很正常" | "刚开始都这样，慢慢来不用急" |
| `avoidance_after_break` | `reset_soft` | 不强调断签，给重新开始的信号 | "好久不见～想从哪开始都行" |
| `high_achievement_fatigued` | `rest_reminder` | 建议休息日 | "最近挺拼的，今天歇一天吧" |
| `social_motivated` | `social_boost` | 给分享/挑战入口 | "要不要把你的记录发出去？" |
| `social_pressured` | `private_default` | 不提排行榜，不用比较语气 | （正常聊天，避谈社交比较） |
| `normal` | `no_intervention` | 常规聊天节奏 | 自然回应，不做额外引导 |

#### 9.6.3 个性化调节示例

同一个"启动困难"，不同心理基线 → 不同干预：

```
用户 A (reward_sensitivity=0.8, pressure_sensitivity=0.2):
  → 给一个小挑战 + 完成后的勋章预告
  "今天走够5000步就解锁新成就～"

用户 B (reward_sensitivity=0.2, pressure_sensitivity=0.8):
  → 强调无压力 + 完全可选
  "今天不想动就不动，没关系的"

用户 C (autonomy_preference=0.9):
  → 给选择权
  "今天有几个选择：散步/拉伸/休息，你想哪个？"
```

### 9.7 与现有系统集成

#### 9.7.1 数据流

```
用户发消息
  │
  ▼
ai_engine.py: _extract_health_data()
  │
  ├─→ 提取健康维度 → 现有流程 (Routine, TrajectoryGraph)
  │
  └─→ [新增] 提取心理维度 → PsychoEvent
        │
        ▼
      psycho_engine.py: ingest_event()
        │
        ├─→ 更新 PsychoState（指数衰减）
        ├─→ 判断可干预状态
        └─→ 推荐干预策略
              │
              ▼
            ResponsePlanner.decide_strategy()
              │
              └─→ 生成回复时注入：当前心理状态 + 干预类型 + 语气建议
```

#### 9.7.2 文件变更清单

| 操作 | 文件 | 内容 |
|---|---|---|
| **新增** | `psycho_engine.py` | PsychoEvent/PsychoState/PsychoTrait/PsychoFingerprint 数据模型 + 心理状态推理引擎 + 可干预状态分类器 + 干预推荐器 |
| **修改** | `ai_engine.py` | 新增 `PSYCHO_EXTRACT_PROMPT`；修改 `_extract_health_data()` 增加心理维度提取 |
| **修改** | `app.py` | 新增 `GET /api/psycho/state`、`GET /api/psycho/fingerprint`；将来可选 `POST /api/psycho/ema` |
| **修改** | `scheduler.py` | 主动触发逻辑参考 `recommend_intervention()` 决定是否开口和说什么 |
| **修改** | `ResponsePlanner` (ai_engine.py) | `decide_strategy()` 纳入心理状态和推荐干预类型 |
| **修改** | `storage.py` | 新增 `psycho_events` / `psycho_states` / `psycho_fingerprints` JSON 存储 |
| **修改** | `trajectory_graph.py` | 可选：让 TrajectoryNode 也承载心理维度的周期性模式 |
| **新增** | `tests/test_psycho_engine.py` | 心理引擎单元测试 |

#### 9.7.3 与图谱可视化的关系

现有的 4 层图谱体系（event/state/trajectory/personality）中，Personality 层的定义从"行为倾向"扩展为"心理基线"。后续考虑在可视化中展示：

- 心理基线的雷达图（7 个维度）
- 心理状态的时序曲线（意愿/疲劳/自我效能的时间变化）
- 可干预状态的日/周标签（"本周主要处于启动困难期"）
- 干预效果反馈（"上次降门槛干预后，用户次日运动概率+30%"）

### 9.8 实施路线图

#### Phase A：心理事件采集管线（预计 1-2 周，可与 Phase 3/4 并行推进）✅ 完成

- [x] `PsychoEvent` 数据模型 + JSON 存储（psycho_engine.py + storage.py）
- [x] `PSYCHO_EXTRACT_PROMPT` 编写 + 独立 extract_psycho_data() 函数（psycho_engine.py）
- [x] EMA 轻量采集：app.py 聊天流程集成（extract → persist → response）
- [x] 单元测试覆盖 10+ 种典型心理表达的提取（39 tests in tests/test_psycho_engine.py）

**Phase A 成功标准：** 用户进行运动相关对话时，能正确提取心理维度（意愿、疲劳、成就感等），准确率 > 70%

#### Phase B：心理状态聚合 + 可干预状态分类（预计 2-3 周）

- [x] `PsychoState` 数据模型 + 指数衰减更新
- [x] `classify_intervenable_state()` 规则引擎（9 种状态）
- [x] `recommend_intervention()` 干预匹配矩阵
- [x] API：`/api/psycho/state`
- [x] API：`/api/psycho/fingerprint`
- [x] 单元测试覆盖所有状态分类路径（25 tests: 9 state rules + 6 intervention + 6 PsychoState model + 4 normalize_dim）

**Phase B 成功标准：** 能从心理事件序列中正确分类可干预状态；干预推荐能在端到端测试中对不同场景给出合理策略

#### Phase C：干预个性化 + ResponsePlanner/Scheduler 集成（预计 2-3 周）

- [x] `PsychoTrait` 数据模型（7 维度基线）+ `compute_psycho_fingerprint()` 启发式推断（6 tests）
- [x] `PsychoFingerprint` 完整建模（μ, Σ, A, B, R）
- [x] ResponsePlanner `decide_strategy()` 纳入心理状态 + 干预类型
- [x] 主动调度器 `should_trigger()` 参考心理状态判断是否触发
- [x] 被动推断信号接入（从 scheduler、routine、plan 中的行为信号推断心理）

**Phase C 成功标准：** AI 的主动触发+回复策略受心理状态显著影响（例如用户在 avoidance_after_break 状态下，AI 不会说"你已经 X 天没动了"）

#### Phase D：MRT 验证框架（后续，Phase 5/6 之后）

- [x] 微随机试验框架：在决策点随机化干预类型
- [x] 近端效果测量：某类干预对某类状态的短期效果
- [x] N-of-1 分析：个体层面最优干预策略学习
- [x] 效果仪表盘：展示心理状态 → 干预 → 行为结果的因果链

### 9.9 关键设计决策（已确认）

1. **EMA 采集方式：** 采用聊天式嵌入，不以直接提问方式获取心理状态。通过情境引导和自然对话让心理信号自然渗出，侵入性降到最低
2. **PsychoTrait 初始化：** 不做固定问卷。在用户对话意愿高时，AI 结合自己的 personality 进行聊天式了解，初期参数默认为 0.5（中性）
3. **与 Persona 系统关系：** 目前独立设计。高压力敏感用户自己大概率不会选毒舌闺蜜。未来如果需要进一步丰富 persona 系统、使 AI 更接近真人时可以考虑接通
4. **数据隐私：** 无需特殊保护策略。将来考虑心理画像的可视化展示，让用户了解自己——这是争取实现"产品比用户更了解自己"愿景的一部分

### 9.10 学术锚点与研究价值

本设计对接以下研究方向：

| 研究方向 | 对应设计 | 参考文献 |
|---|---|---|
| **JITAI** (Just-in-Time Adaptive Interventions) | 整体架构：根据个体变化的内部状态在合适时间给出合适支持 | [PMC 5364076](https://pmc.ncbi.nlm.nih.gov/articles/PMC5364076/) |
| **EMA** (Ecological Momentary Assessment) | 心理事件采集：在真实场景中捕捉瞬时心理状态 | [PMC 5161656](https://pmc.ncbi.nlm.nih.gov/articles/PMC5161656/) |
| **自我效能动态波动** | PsychoState 中 self_efficacy 的日内/日常波动追踪 | [PMC 12882901](https://pmc.ncbi.nlm.nih.gov/articles/PMC12882901/) |
| **Digital Phenotyping** | 被动推断：从手机/可穿戴/交互行为推断心理状态 | [PMC 10753422](https://pmc.ncbi.nlm.nih.gov/articles/PMC10753422/) |
| **MRT** (Micro-Randomized Trials) | Phase D 验证框架：在决策点反复随机化干预 | [arXiv 2007.13741](https://arxiv.org/abs/2007.13741) |

**核心研究方向命名：**

> **Psychological Trajectory-Aware JITAI for Physical Activity Adherence**
>
> 中文：**面向运动坚持的心理轨迹感知式即时自适应干预**

**创新点：**

1. 不把用户心理看成静态标签，而是建模为动态轨迹
2. 不只预测是否运动，而是估计用户当前可干预状态
3. 不只个性化内容，还个性化时机、强度、语气和目标难度
4. 不把成就感当唯一机制，而是区分疲劳、压力、自我效能、控制感、回避倾向等细粒度状态
5. 可以用 MRT 或 N-of-1 设计验证某类干预对某类状态是否有效

**最小研究问题：**

> Can fine-grained psychological trajectories improve the timing and personalization of physical activity interventions beyond static user profiles and post-exercise achievement feedback?

> 相比静态用户画像和运动后成就反馈，细粒度心理轨迹是否能更好地指导运动干预的时机与内容个性化？

---

## 10. 竞争分析

### 市场格局

```
主动智能 ─── 被动响应
   │            │
 Viora      Apple Health
 (主动关心)    Flux 健康日记
              BoDo 波度
              MeYou Health
              Headspace / Calm
```

### 竞品对比

| 维度 | Apple Health | Flux 健康日记 | BoDo 波度 | Viora |
|---|---|---|---|---|
| **交互模式** | 用户手动记录 | 每日填表 | 问卷打卡 | **AI 主动找你聊天** |
| **录入方式** | 手动 + 自动同步 | 每日填表 | 问卷打卡 | **朋友式对话（悄无声息收集）** |
| **游戏化程度** | 无 | 中（连续记录提示） | 高（打卡、徽章） | **零** |
| **AI 角色** | 无 | 无 | 无 | **主动关心的熟人朋友** |
| **Persona** | 无 | 无 | 无 | **有性格、可自定义** |
| **记忆能力** | 仅数据记录 | 仅数据记录 | 仅数据记录 | **长期记忆、熟人关系** |
| **数据叙事** | 无 | 周报（表格为主） | 有，但偏报告 | **身体故事** |
| **关联洞察** | 无 | 无 | 无 | **图谱关联 + AI** |
| **目标人群** | 所有人 | 关注健康的年轻人 | 减脂人群 | **亚健康的都市青年** |
| **中国本土化** | 弱 | 中 | 中 | **强** |

### Viora 的竞争壁垒
1. **品类定义权：** 第一个以"主动关心"为核心交互模式的健康产品
2. **主动智能 vs 被动响应：** 市场上所有竞品都在等用户主动，Viora 先迈出第一步
3. **熟人关系壁垒：** 随着使用时间增长，Viora 对用户了解越来越深，迁移成本越来越高
4. **反 gamification 立场：** 真正的零压力，不是嘴上说说不做而已
5. **悄无声息的信息收集：** 用户不觉得在记录，但数据自然积累
6. **图谱叙事：** 独有的健康关联可视化和故事化能力

---

## 11. 风险分析

| 风险 | 可能性 | 影响 | 缓解措施 |
|---|---|---|---|
| **AI 太主动，用户觉得像骚扰** | 高 | 高 | 频率控制（每天最多一次）；沉默模式；退让机制；连续不回应自动降低频率；用户可自定义互动频率 |
| Apple 加入主动式健康追踪 | 中 | 高 | Apple 的优势是硬件数据层，我们的优势是叙事层、图谱和 Persona。先建立用户认知和习惯。 |
| 用户获客成本过高 | 高 | 中 | 先做免费工具，通过产品内身体故事分享获取自然流量。不依赖付费广告。 |
| AI 洞察不准确 | 高 | 中 | 描述性先于处方性；所有洞察附免责声明；标注置信度和记录数；用户可随时纠正 AI。 |
| 用户不愿意持续交互 | 中 | 高 | 朋友式聊天降低交互门槛；零压力设计；反馈足够有趣让用户愿意回来；AI 主动找你减少用户主动打开的阻力。 |
| 图谱抢走主玩法注意力 | 低 | 中 | 图谱不做强入口，作为"想看的时候再看"的展示层；主入口永远是聊天框。 |
| 产品变成另一个打卡软件 | 中 | 高 | 从产品原则层面禁止：不做 streak、不做连续天数、不做施压式推送。每个新功能加一个检查：这个功能会不会给用户压力？ |
| 用户隐私和数据安全 | 中 | 高 | 本地存储优先；数据不出设备；如果需要云端同步，用户明确授权后才上传。 |
| Persona 定制过度导致失控 | 低 | 中 | 在 Persona 系统层面设置安全边界；不允許用户将 AI 设定为给出医疗建议的角色 |

---

## 12. 技术要求

### 当前技术栈
- **后端：** Python + Flask
- **前端：** HTML/CSS/JavaScript + D3.js（图谱可视化）
- **数据存储：** 本地 JSON / SQLite（MVP 阶段）
- **AI 提取：** LLM API（用于自然语言 → 结构化数据提取 + Persona 驱动对话）

### 扩展要求
- **时间感知（Phase 1）：** 每次 API 调用时在 system prompt 中注入当前时间（日期、星期、时间段），让 AI 能做出与时间匹配的回复
- **天气 API 集成（Phase 2）：** 接入免费天气 API（如和风天气、OpenWeatherMap），用户授权位置后注入天气、温度、空气质量等环境上下文
- **Routine 学习（Phase 2）：** 实现用户日常作息建模和异常检测算法
- **主动交互调度（Phase 2）：** 基于 routine、时间、环境的触发引擎
- **Persona 系统（Phase 2）：** Persona 配置 + 动态 Prompt 生成
- **关联计算（Phase 2）：** 实现简单的统计关联算法（Pearson / Spearman 相关系数）
- **图谱算法：** 支持动态权重更新、时间分片图谱
- **用户数据模型：** 设计可扩展的数据模型，支持多维度、多来源数据
- **语音处理（Phase 3）：** 集成语音转文字引擎（如 Whisper），支持普通话

### 非功能性要求
- 响应时间：聊天回复 < 3 秒（AI 提取）
- 主动消息延迟：触发后 5 分钟内送达
- 离线可用：核心记录功能可离线工作
- 数据可导出：用户可随时导出全部数据（JSON 格式）

---

## 13. MVP 成功指标

### Phase 1 成功标准
- [x] 用户能用一句话完成一次健康记录
- [x] AI 提取准确率 > 80%（50 条测试用例）
- [x] 用户能感受到 AI 的性格和语气（非冷冰冰的机器回复）
- [x] 体检数据导入后可自动生成图谱骨架
- [x] 时间线叙事可读、有趣、不枯燥
- [ ] 用户不会感受到任何压力（零 gamification）

### Phase 2 成功标准
- [ ] Routine 学习在 2 周后能识别 5+ 个日常维度
- [ ] 主动交互触发准确率 > 70%（用户认为"找得准"）
- [ ] 用户不觉得被骚扰（调研中 80%+ 认为主动频率合适）
- [ ] 图谱关联计算可用，用户能看到"睡眠-精力"等维度的关联强度
- [ ] AI 趋势洞察基于真实数据生成
- [ ] Persona 自定义可用，用户能切换不同角色风格
- [ ] 身体故事包含关联引用

### Phase 3 成功标准
- [ ] 多渠道输入（文本 + 语音 + 图片 + WeChat）全部可用
- [ ] 身体故事可导出和分享
- [ ] 多渠道主动交互可用
- [ ] 多体检对比可用
- [ ] 用户留存：30% 用户在 30 天后有再次使用记录

### 通用产品指标（长期）
- **核心行为触发：** 用户第一次跟 Viora 说一句话到感受到"被关心" < 30 秒
- **主动交互接受率：** AI 主动发起的消息中，用户回复的比例 > 50%
- **回看率：** 记录后 7 天内回看身体故事的用户比例 > 40%
- **无压力验证：** 用户调研中 80%+ 认为"这个产品没有给我任何压力"
- **熟人感验证：** 用户调研中 70%+ 认为"Viora 像是认识我一段时间的朋友"
- **口碑传播：** 自然分享率 > 10%（身体故事被分享的比例）

---

## 14. 结论

Viora 最重要的不是"记得多"，而是**"有人主动关心你、像熟人一样懂你、让你愿意跟它聊下去"**。

它不需要打败 Apple Health，也不需要替代任何临床工具。它的存在是为了服务一个被忽视的人群：**70%+ 有亚健康困扰、但不想被健康管理工具绑架的都市年轻人**。

通过主动智能、熟人关系、悄无声息的信息收集、有性格的 AI 朋友、图谱叙事和 AI 洞察，Viora 要占据一个全新的品类：**proactive AI body companion — 主动式 AI 身体搭子**。

> 不是健康应用。是主动关心你的身体搭子。

